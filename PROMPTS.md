# PROMPTS.md — 프롬프트 & 리뷰 로그

AI(Claude Opus 5, Claude Code CLI)와 협업한 기록이다.
기능 단위로 쪼개 지시했고, 커밋 1개 = 기능 1개 = 아래 섹션 1개로 대응된다.

각 섹션은 다음 형식이다.

- **프롬프트** — AI에 준 지시 원문 (요약 아님)
- **AI 출력 요지** — 무엇을 내놓았나
- **리뷰 / 검증** — 무엇이 문제였고, 어떻게 알아냈고, 어떤 판단을 왜 했나

> 검증 항목은 실제로 실행·재현해서 확인한 것만 적었다.
> 확인하지 못한 항목은 `미검증`으로 명시했다. 추측을 검증으로 쓰지 않았다.

---

## [setup] 프로젝트 세팅

### 프롬프트 1

```
채용 파이프라인 보드를 만든다. 코딩테스트 과제고 제약이 있다.

- Vite + React 19 + TypeScript + Tailwind CSS
- 테스트: Vitest + React Testing Library + jsdom
- 상태관리 라이브러리 없음. useReducer + Context로 직접 짠다.
- TanStack Query 쓰지 않는다. 낙관적 업데이트/롤백이 이 과제의 평가 대상이라
  라이브러리가 대신하면 안 된다.

지금은 세팅만 해라. 앱 코드는 다음 단계에서 쓴다.
1) package.json / vite.config.ts / tsconfig / tailwind 설정
2) vitest 설정 (jsdom, setup 파일에 @testing-library/jest-dom)
3) eslint + prettier (규칙 과하게 넣지 마라)
4) src 폴더 구조만 빈 디렉터리로:
   features/board, features/applicants, mocks, lib, components
5) npm run dev / test / build 스크립트

.gitignore 포함. App.tsx는 "보드 준비 중" 한 줄 플레이스홀더면 된다.
```

### AI 출력 요지

`package.json`(scripts만) → 의존성은 `npm install`로 최신 버전 해석하도록 하고,
`vite.config.ts`(vitest 설정 통합), `tsconfig.json`(strict + `noUncheckedIndexedAccess`),
`eslint.config.js`(flat config), `.prettierrc`, `index.html`,
`src/{main.tsx,App.tsx,index.css,vite-env.d.ts,test/setup.ts}` 생성.
Tailwind는 v4의 `@tailwindcss/vite` 플러그인 + CSS에 `@import 'tailwindcss'` 방식 선택.

### 리뷰 / 검증

**1) TypeScript 7이 설치되어 ESLint 툴체인이 깨졌다 — 버전을 내려 해결**

- **무엇이 문제였나**: 의존성 버전을 손으로 적지 않고 `npm install`에 맡겼는데,
  npm이 `typescript@7.0.2`를 설치했다. 이어서 lint 의존성을 설치하니 ERESOLVE로 실패.
- **어떻게 알아냈나**: `npm install -D typescript-eslint` 가 exit 1. 에러 원문:
  ```
  Found: typescript@7.0.2
  peer typescript@">=4.8.4 <6.1.0" from typescript-eslint@8.69.0
  ```
- **판단**: `--legacy-peer-deps`로 넘기지 않고 **TypeScript를 `~5.9`로 내렸다.**
  TS 7은 신규 컴파일러라 lint 생태계가 아직 못 따라온다. 채용 과제에서
  "최신 버전을 썼다"는 것보다 툴체인이 안 깨지는 게 중요하다고 봤다.
  `--legacy-peer-deps`는 문제를 숨기는 쪽이라 기각.

**2) `node:url` 타입 누락으로 `tsc --noEmit` 실패 — AI 초안의 실제 누락**

- **무엇이 문제였나**: AI가 `vite.config.ts`에서 경로 alias를 위해
  `import { fileURLToPath } from 'node:url'`을 썼는데 `@types/node`를 빼먹었다.
- **어떻게 알아냈나**: `npm run build` 실행 →
  `vite.config.ts(4,31): error TS2307: Cannot find module 'node:url'`.
  vite build 자체는 통과하므로 `tsc --noEmit`을 build 스크립트에 넣어두지 않았다면
  못 잡았을 오류다.
- **어떻게 고쳤나**: `@types/node`를 devDependency로 추가.

**3) `eslint-plugin-react-hooks` v7의 flat config 경로가 바뀌어 있었다**

- **무엇이 문제였나**: AI가 `reactHooks.configs['recommended-latest']`를 썼다.
  이는 v5 기준의 관용구인데, v7에서는 이 키가 여전히 **eslintrc(legacy) 형식**이다.
  ESLint 10은 `plugins`가 배열이면 거부한다:
  `A config object has a "plugins" key defined as an array of strings.`
- **어떻게 알아냈나**: `npm run lint` 실패 → 플러그인의 export를 직접 열어 확인.
  ```
  node -e "import('eslint-plugin-react-hooks').then(m=>{...})"
  recommended        | plugins: ARRAY(legacy)
  recommended-latest | plugins: ARRAY(legacy)
  flat               | keys: recommended-latest, recommended
  ```
  즉 flat config용은 `configs.flat['recommended-latest']` 아래로 이동했다.
- **어떻게 고쳤나**: 해당 경로로 교체하고, 왜 그 경로인지 코드에 주석으로 남겼다.
  (다음에 버전을 올릴 때 다시 헤매지 않도록)

**4) 테스트 0개일 때 `npm run test`가 죽는 문제 — 선제 처리**

- `vitest run`은 테스트 파일이 없으면 exit 1이다. 커밋 0~6은 테스트가 없으므로
  세팅 단계에서 `npm run test`가 실패하게 된다.
- `passWithNoTests: true`를 설정하고, **실제로 exit code 0인지 확인**했다:
  `No test files found, exiting with code 0`.

**5) Tailwind v4가 실제로 동작하는지 확인 (설정만 보고 넘기지 않았다)**

- Tailwind v4는 v3와 설정 방식이 다르다(`tailwind.config.js` + `content` 배열이 아니라
  Vite 플러그인 + CSS `@import`). 설정이 그럴듯해 보여도 클래스가 안 먹을 수 있다.
- `npm run build` 후 **산출된 CSS를 직접 grep** 해서 확인:
  ```
  min-h-dvh          1
  place-items-center 1
  bg-slate-100       1
  text-slate-600     1
  ```
  4개 모두 존재 → JIT 스캔이 `src`를 제대로 훑고 있다. CSS 4.63kB.

**최종 상태**: `npm run build` / `test` / `lint` 3개 모두 통과.

**미검증**: `npm run dev`의 HMR 동작. (dev 서버는 이후 UI 커밋에서 실제로 띄워 확인할 예정)

---

## [mock-api] 목 API 자체 구현

### 프롬프트 1

```
mock API를 자체 구현한다. 실제 백엔드는 없다.

도메인
- Applicant: id, name, position(직무), appliedAt(지원일), stage, version,
  상세용 필드 email, phone, experienceYears, resumeSummary, memo
- Stage: 'screening'(서류검토) | 'interview'(면접) | 'offer'(처우협의)
  | 'hired'(최종합격) | 'rejected'(불합격)

mock 서버 (src/mocks/)
- 모든 응답에 200~800ms 랜덤 지연
- 약 15% 확률로 실패
- 저장소는 localStorage. 새로고침 후에도 이동 결과가 유지돼야 한다.
- 시드 고정 PRNG로 지원자를 결정론적으로 생성. 건수는 200 / 1000 전환 가능하게.
- 엔드포인트:
  listApplicants()
  getApplicant(id)
  moveApplicantStage({ id, toStage, expectedVersion })
- moveApplicantStage는 expectedVersion이 서버 version과 다르면
  409 개념의 ConflictError를 던지고 서버의 현재 상태를 함께 담아 반환한다.
  성공하면 version을 +1 한다.
- 지연과 실패확률을 테스트에서 강제 주입/고정할 수 있게 설계해라.
  전역 Math.random 몽키패치 말고 명시적 config 주입 방식으로.

한글 이름과 직무는 그럴듯하게 생성. 지원일은 최근 90일 범위.
```

### AI 출력 요지

`src/domain/applicant.ts`(타입·STAGES), `src/mocks/`에
`random.ts`(mulberry32 PRNG), `config.ts`(주입 가능한 설정),
`seed.ts`(결정론적 생성), `store.ts`(메모리 + localStorage), `errors.ts`
(MockApiError/NetworkError/NotFoundError/ConflictError), `api.ts`(3개 엔드포인트) 생성.
설정은 `configureMock(patch)` / `resetMockConfig()`로 주입하고,
실패 판정은 `failureRate`가 0이나 1이면 난수를 아예 건드리지 않게 해서
테스트가 난수 스텁 없이도 결정론적이 되도록 했다.

### 리뷰 / 검증

**1) 이름 음절 배열에 한글 아닌 문자가 섞여 있었다**

- **무엇이 문제였나**: `GIVEN_FIRST`에 `'نا'`(아랍 문자), `GIVEN_SECOND`에 `'訓'`(한자)이
  들어가 있었다. 그대로 두면 "김نا훈" 같은 이름이 생성된다.
- **어떻게 알아냈나**: 눈으로 훑다가 발견하고, 확인 사살로 파일 전체를 코드포인트로 스캔했다.
  `한글 음절(AC00~D7A3)이 아닌 U+2500 이상 문자`를 뽑는 스크립트를 돌려 0개가 될 때까지 고쳤다.
- **판단**: 수정. 이런 종류는 리뷰로만 잡으면 놓치기 쉬워서, 사람 눈이 아니라 스캔으로 확인했다.

**2) 테스트가 실제로 이빨이 있는지 — 구현을 일부러 깨서 확인했다 (변이 테스트)**

테스트 17개가 **첫 실행에 전부 통과**했다. 이건 좋은 신호가 아니라 의심할 지점이다.
테스트가 느슨해서 통과한 건지 구별해야 하므로, 구현을 의도적으로 망가뜨려
테스트가 잡아내는지 확인했다.

- 변이 A: `nextLatencyMs`를 `minLatencyMs + random()*(max-min)` →
  `random() * maxLatencyMs`로 바꿈 (= 흔한 실수. 0ms가 나와 "200~800ms" 요구 위반)
  → **2개 실패**: `기본 설정에서 항상 200~800ms 범위다`, `난수가 0이어도 하한(200ms)을 지킨다`
- 변이 B: `store.readAll()`의 방어 복사(`.map(a => ({...a}))`)를 제거
  → **1개 실패**: `밖으로 나간 객체를 변형해도 저장소가 오염되지 않는다`

둘 다 잡혔으므로 해당 테스트는 유효하다. 변이 후 원복하고 전체 재실행해 17/17 통과 확인.

**3) 실패 판정 시점을 "쓰기 전"으로 한정한 것 — AI 제안 수용, 다만 이유를 따져봤다**

- mock 서버가 실패할 때 (a) 요청이 서버에 닿지 못함 (b) 서버는 처리했는데 응답이 유실됨
  두 가지가 가능하다. AI 초안은 (a)였다.
- **검증**: (b)를 흉내내면 클라이언트가 반영 여부를 알 방법이 없어 낙관적 롤백이
  정답 없는 문제가 된다. 과제 요구는 "실패 시 원상 복구"이므로 (a)가 요구에 맞다.
- **채택**하고 `api.ts`의 `simulateNetwork` 주석에 이 판단을 남겼다.
  `실패한 요청은 서버 상태를 바꾸지 않는다` 테스트로 못 박아뒀다.

**4) 직접 확인한 나머지 동작**

`npm run test` 로 실제 검증한 항목:
- 같은 `(seed, count)` → 결과 완전 일치 / 다른 seed → 불일치
- 기본 설정에서 지연이 2,000회 모두 200~800ms 범위
- `failureRate: 1` → 100회 모두 실패, `0` → 100회 모두 성공
- 기본 실패율 20,000회 표본에서 0.14~0.16 (요구 "약 15%") 
- 이동 성공 시 `version` +1, `stageHistory` +1, `from`/`to` 정확
- **이동 결과가 localStorage에 실제로 쓰이는지** — `localStorage`를 직접 파싱해서 확인.
  (mock API가 메모리만 갱신하고 영속화를 빼먹는 건 흔한 누락이라 별도 테스트로 고정)
- 낡은 `expectedVersion` → `ConflictError`, `status 409`, `current`에 서버 최신 상태 포함
- 같은 단계로의 이동은 `version`을 올리지 않음
- 실패한 이동 후 서버 상태 불변

**5) 계획에서 벗어난 점 (의도적)**

- `stageHistory`를 커밋 9(상세 패널)에서 추가하려던 계획을 **커밋 1로 앞당겼다.**
  이유: 1,000건이 이미 localStorage에 쓰인 뒤 필드를 추가하면 스키마 마이그레이션이
  필요해진다. `moveApplicantStage`가 유일한 변경 지점이니 처음부터 기록하는 게 싸다.
  대신 스키마 키에 버전을 박아(`hpb.applicants.v1`) 나중에 바꿀 때 옛 데이터를
  버릴 수 있게 했다.
- mock API 테스트를 커밋 7(테스트 커밋)까지 미루지 않고 이 커밋에 포함했다.
  mock 서버가 이후 모든 기능의 토대이므로, 여기가 틀리면 뒤가 전부 틀린다.

**미검증**: 브라우저에서의 실제 새로고침 유지. (다음 커밋에서 UI가 붙으면 확인)

---

## [board-layout] 5단계 컬럼 보드

### 프롬프트 1

```
보드 레이아웃을 만든다. src/mocks의 mock API를 쓴다.

- ApplicantsProvider: useReducer + Context.
  state = { status: 'idle'|'loading'|'error'|'ready', byId, allIds, error }
  액션은 지금 LOAD_START / LOAD_SUCCESS / LOAD_ERROR 만.
  정규화(byId + allIds)로 저장해라. 나중에 카드 단위 낙관적 업데이트를 할 거라서
  배열 순회로 찾는 구조면 안 된다.
- Board: 5개 컬럼 가로 배치. 데스크톱 5열, 좁아지면 가로 스크롤.
- Column: 제목 + 건수 배지 + 세로 스크롤 영역. 마크업은 ul/li 시맨틱으로.
- 단계 라벨/색/순서는 한 곳(constants)에서 관리.
  순서는 서류검토 → 면접 → 처우협의 → 최종합격 → 불합격.
- 지금은 카드 내용 없이 자리만. 로딩/에러/빈 상태는 다음 단계다.

Context 값은 state와 dispatch를 분리해서 노출해라. 리렌더 범위 때문에.
```

### AI 출력 요지

`domain/stages.ts`(라벨·색·순서), `features/applicants/`에 `types.ts`·`reducer.ts`·
`ApplicantsProvider.tsx`, `features/board/`에 `Board.tsx`·`Column.tsx`·`selectors.ts`.
Context를 `StateContext` / `ActionsContext` 둘로 쪼개고, 후자에는 `reload`만 노출.
셀렉터는 컬럼별 `filter` 5회 대신 전체를 한 번 훑어 단계별로 갈라 담는 방식(`groupIdsByStage`).

### 리뷰 / 검증

**1) 데이터는 있는데 status가 error인 상태가 실제로 발생했다 — 낡은 응답이 최신 상태를 덮어씀**

- **무엇이 문제였나**: 브라우저에서 처음 띄웠을 때 컬럼 건수는 정상(합계 200)인데
  헤더의 "지원자 200명"이 안 나왔다. 즉 데이터는 채워졌는데 `status !== 'ready'`였다.
- **어떻게 알아냈나**:
  1. DOM을 직접 조회해 컬럼 합계가 200임을 확인 (`sum: 200`) → 데이터는 들어왔다.
  2. 헤더 `<p>` 내용이 빈 문자열임을 확인 → 조건 `status === 'ready'`가 거짓이다.
  3. 상태를 눈으로 볼 수 없어서 루트 div에 `data-load-status` 속성을 붙여 노출시켰다.
  4. 리듀서를 다시 읽어보니 `LOAD_ERROR`가 `byId`를 지우지 않는다(의도한 것).
     그래서 "성공 후 실패"가 겹치면 데이터 + error 조합이 만들어진다.
- **원인**: React StrictMode가 개발 모드에서 effect를 두 번 실행한다.
  → `listApplicants()`가 2회 나가고, 각각 15% 확률로 실패한다.
  → 1번이 성공하고 2번이 실패하면 데이터는 남고 status만 error가 된다.
  (약 28% 확률로 뭔가 어긋난다. 실제로 첫 실행에 바로 걸렸다.)
- **어떻게 고쳤나**: AI 초안에 없던 **요청 시퀀스 가드**를 넣었다.
  `loadSeq` ref를 두고, 응답이 도착했을 때 자기 순번이 최신이 아니면 dispatch를 버린다.
  이제 겹친 로드 중 **마지막 요청의 결과만** 반영된다.
- **재검증**: 4회 재적재 + `?count=1000` 전환까지 모두 `status: ready`,
  합계는 각각 200 / 1000으로 정확했다.
- **남은 판단**: 시퀀스 가드를 넣어도 "마지막 요청이 실패하면 error"는 여전히 맞는 동작이다.
  다만 **이미 데이터가 있는데 재적재만 실패한 경우 보드를 지워버리는 건 나쁜 UX**다.
  이건 다음 커밋(loading-error-empty)에서 "데이터가 없을 때만 전체 에러 화면,
  있으면 비차단 배너"로 처리한다.

> 이 문제는 커밋 11(경쟁 상태)에서 다룰 "늦게 온 응답이 최신 상태를 덮어쓴다"와
> **같은 종류**다. 카드 이동에서만 생기는 줄 알았는데 초기 로드에서 먼저 터졌다.

**2) 컬럼별 `filter` 5회를 한 번 순회로 바꿨다**

- AI 초안은 컬럼마다 `allIds.filter(id => byId[id].stage === stage)`였다.
  1,000건 × 5컬럼 = 5,000회 순회 + 배열 5개 생성이 매 렌더마다 일어난다.
- `groupIdsByStage`로 전체를 한 번만 훑도록 바꾸고 `useMemo`로 감쌌다.
- 아직 체감 성능 문제는 없지만 커밋 8(검색/필터)에서 매 입력마다 재계산되는 자리라
  미리 정리해 두는 게 맞다고 판단했다.

**3) 브라우저에서 직접 확인한 것**

- 컬럼 순서: `서류검토 → 면접 → 처우협의 → 최종합격 → 불합격` (constants 순서와 일치)
- 컬럼 건수 합 = 전체 건수 (200 / 1000 둘 다)
- `aria-label`이 `"서류검토 89명"` 형태로 단계명 + 건수를 담고 있음
- `?count=1000` 전환 시 localStorage가 재시드되고 합계 1000
- 이 시점 DOM 노드 수 **46개** (카드가 아직 없으므로) — 커밋 13 가상화 비교용 기준선은
  카드가 붙는 다음 커밋에서 다시 측정한다.

**미검증**: `state`/`actions` Context 분리가 실제로 리렌더를 줄이는지.
(Profiler 측정은 카드가 붙고 필터가 생기는 커밋 8에서 의미가 있다)

---

## [card-list] 지원자 카드

### 프롬프트 1

```
ApplicantCard를 만든다.

- 표시: 이름, 직무, 지원일(YYYY.MM.DD), 현재 단계 배지
- 지원일 포맷은 Intl.DateTimeFormat ko-KR. dayjs 같은 라이브러리 추가하지 마라.
- 카드는 포커스 가능하게 (다음 단계에서 상세 패널을 열 거다). 지금은 포커스 링만 제대로.
- ApplicantCard는 React.memo. props로 applicant 객체 하나만 받게.
- 컬럼은 이제 실제 카드를 렌더한다.
  stage별 필터링은 셀렉터 함수로 분리해라 (features/board/selectors.ts).

디자인은 과하지 않게. 밀도 있는 실무용 보드 느낌으로.
```

### AI 출력 요지

`lib/format.ts`(모듈 스코프 `Intl.DateTimeFormat` 재사용), `ApplicantCard.tsx`(memo),
셀렉터를 `groupIdsByStage` → `groupByStage`(id 대신 객체 배열 반환)로 변경.
카드는 `li > button` 구조 — 커밋 5에서 "단계 이동" 버튼을 형제로 넣을 자리를 남기려고
버튼 안에 버튼을 중첩하지 않는 구조로 잡았다.

### 리뷰 / 검증

**1) `Intl` 인스턴스 생성 위치 — AI가 처음부터 모듈 스코프에 뒀다. 채택**

- 카드 렌더 함수 안에서 `new Intl.DateTimeFormat(...)`을 만들면 1,000건에서
  1,000개가 매 렌더마다 생성된다. AI 초안은 모듈 스코프에 한 번만 만들어 재사용했다.
- **검증**: 코드 위치 확인 + `formatAppliedDate` 단위 테스트 3개 추가.
  ko-KR은 `"2026. 09. 07."`처럼 공백과 끝 점을 넣는데, 이걸 정리하는 로직이
  실제로 맞는지(공백 없음/끝 점 없음/한 자리 월일 zero-pad) 테스트로 고정했다.
  `\s`가 NBSP(U+00A0)까지 잡는지가 관건이라 정규식만 보고 넘기지 않고 테스트로 확인.

**2) `columnClientHeight: 8` — 버그로 의심했으나 측정 오류였다**

- 1,000건에서 컬럼 스크롤 영역을 재보니 `clientHeight: 8`, `scrollHeight: 33153`이
  나왔다. 컬럼이 찌그러진 것으로 보였다.
- **어떻게 확인했나**: 스크린샷을 찍어보니 화면은 정상이었다. 다시 재보니 `586px`.
  원인은 내 측정 코드였다 — 긴 이름을 `textContent`로 강제 주입한 직후에 측정해서
  레이아웃이 확정되기 전 값을 읽었다.
- **판단**: 코드 수정 없음. **측정값이 이상하면 코드를 고치기 전에 측정을 의심해야 한다는
  걸 그대로 겪었다.** 여기서 바로 CSS를 건드렸으면 정상 코드를 망가뜨렸을 것이다.

**3) 긴 이름·긴 직무명 레이아웃 — 직접 주입해서 확인**

- DOM에 `'김아주아주아주아주긴이름테스트입니다'`를 강제로 넣고 측정.
- 카드 폭 변화 없음(231px → 231px), `scrollWidth > clientWidth`로 **말줄임 처리 확인**.
  스크린샷에서도 `김아주아주아주아주긴...`으로 잘려 나온다.
- 시드 데이터는 3글자 이름만 만들어서 이 케이스가 자연 발생하지 않는다.
  그래서 강제 주입으로 확인했다.

**4) 1,000건 기준선 측정 (커밋 13 가상화 비교용)**

| 항목 | 200건 | 1,000건 |
| --- | --- | --- |
| 렌더된 카드 `li > button` | 200 | 1,000 |
| 전체 DOM 노드 | 1,646 | **8,046** |
| 컬럼 `scrollHeight` (서류검토) | — | 33,153px |

컬럼별 건수: 445 / 250 / 115 / 84 / 106 = 합계 1,000 ✓

**5) `React.memo`가 실제로 효과가 있는지 — 이 커밋에서는 검증 불가**

- memo의 효과는 "카드 한 장만 갱신될 때 나머지는 리렌더되지 않는다"인데,
  아직 카드 단위 변경(단계 이동)이 없다. 전체 재적재는 모든 객체가 새 참조가 되므로
  전부 리렌더되는 게 정상이고, 이걸로는 memo를 검증할 수 없다.
- **미검증 → 커밋 6(낙관적 업데이트)으로 넘긴다.** 그때 카드 1장 갱신 시
  리렌더 범위를 실제로 측정한다.
- 지금 확인한 것은 전제 조건뿐이다: prop이 `applicant` 객체 하나이고,
  그 객체가 정규화 스토어의 `byId`에서 그대로 오므로 참조가 안정적이다.
  (인라인 객체·화살표 함수를 prop으로 넘기지 않았음을 코드로 확인)

---

## [loading-error-empty] 로딩 / 에러 / 빈 상태

### 프롬프트 1

```
로딩/에러/빈 상태를 UI로 처리한다. 빈 상태는 3가지로 구분해라.

1) 로딩: 컬럼 5개 + 카드 스켈레톤. 스피너 하나로 때우지 마라.
2) 에러: 전체 로드 실패 시 보드 대신 에러 패널 + "다시 시도" 버튼.
   mock API가 15% 실패하니 실제로 자주 보인다.
3) 빈 상태 3분기
   - 데이터 자체가 0건 → 보드 전체 빈 상태
   - 특정 컬럼이 0건 → 컬럼 안에 "해당 단계 지원자 없음"
   - 필터 결과가 0건 → (필터는 다음 커밋이지만 자리를 미리 만들어라)
     "조건에 맞는 지원자 없음" + 필터 초기화 버튼

prefers-reduced-motion에서 스켈레톤 애니메이션 끄는 것 포함.
```

### AI 출력 요지

`components/EmptyState.tsx`(공통 표현), `features/board/BoardSkeleton.tsx`,
`features/board/BoardError.tsx`(전체 패널 + 재적재 배너), `Column`에 컬럼 빈 상태,
`Board`에 상태 분기. `mocks/config.ts`에 `?fail=`, `?count=0` 쿼리 지원 추가.

### 리뷰 / 검증

**1) `isRetrying` prop이 죽은 코드였다 — 실제로 눌러보니 드러났다**

- **무엇이 문제였나**: AI 초안은 `BoardError`/`ReloadErrorBanner`에
  `isRetrying` prop을 받아 버튼 라벨을 `"불러오는 중…"`으로 바꾸고 disabled 처리했다.
  그럴듯해 보였지만 **이 prop은 항상 false다.** 두 컴포넌트는 `status === 'error'`일 때만
  렌더되는데 `isRetrying`은 `status === 'loading'`이라서 동시에 참일 수 없다.
- **어떻게 알아냈나**: 코드를 읽고 안 게 아니라 **브라우저에서 재시도를 실제로 눌러서**
  알았다. 클릭 전후 상태를 찍어보니 `error → loading → error`로 전이하는데,
  `"불러오는 중"` 라벨을 가진 버튼을 찾으면 `null`이었다:
  ```
  { before: "error", during: "loading",
    disabledDuringRetry: null, labelDuring: null, after: "error" }
  ```
  재시도를 누른 순간 status가 loading이 되어 **에러 패널 자체가 스켈레톤으로 교체**되고,
  버튼이 언마운트된 것이다.
- **어떻게 고쳤나**: 두 컴포넌트에서 `isRetrying`을 제거하고, 진행 표시를
  **헤더의 "다시 불러오기" 버튼으로 옮겼다.** 헤더는 어떤 상태에서도 살아 있으므로
  최초 로드/재적재 양쪽 모두를 표현할 수 있다.
- **왜 중요한가**: "미사용 코드 방치"는 이 과제의 명시된 감점 신호다.
  타입이 맞고 화면도 그럴듯하면 리뷰만으로는 안 걸린다. 눌러봐야 나온다.

**2) 재적재 실패 시 보드를 지우지 않도록 — 커밋 2에서 남긴 숙제를 해결**

- 커밋 2에서 "데이터가 있는데 재적재만 실패하면 보드를 지워버리는 건 나쁜 UX"라고
  적어뒀던 문제다. 이번에 분기를 둘로 나눴다.
  - 데이터 없음 + 실패 → 전체 에러 패널 (보드 대신)
  - 데이터 있음 + 실패 → 비차단 배너 + 보드 유지
- **검증**: 초기 로드를 성공시킨 뒤 "다시 불러오기"를 눌러 실패를 유도했다(기본 15%,
  1회 시도에 걸림). 결과:
  ```
  status: "error"
  banner: "목록을 새로 불러오지 못했습니다. 화면의 내용이 최신이 아닐 수 있습니다. (…)"
  cardsStillVisible: 200
  columnsStillVisible: 5
  ```
  에러 상태인데도 카드 200장과 컬럼 5개가 그대로 살아 있음을 확인했다.

**3) 실패/빈 상태를 URL로 재현 가능하게 만들었다**

- 15%는 "때때로 실패"라서 에러 UI를 눈으로 확인하기 어렵다. 확인하려고 소스를
  임시 수정하는 건 검증 방식으로 나쁘고, 평가자도 재현할 수 없다.
- `?fail=1`(항상 실패) / `?fail=0`(절대 실패 안 함) / `?count=0`(0건) /
  `?count=3`(일부 컬럼만 0건) 를 지원하게 했다. README에도 적을 예정.

**4) 브라우저에서 실제로 확인한 상태 4종**

| 케이스 | URL | 확인 결과 |
| --- | --- | --- |
| 로딩 스켈레톤 | `?fail=1` (초기 120ms) | `status: loading`, 스켈레톤 컬럼 5개, 펄스 요소 27개 |
| 전체 에러 + 재시도 | `?fail=1` | 에러 패널 + `role="alert"` 존재, 재시도 클릭 시 `error→loading→error` |
| 빈 상태 ① 0건 | `?count=0` | "등록된 지원자가 없습니다" |
| 빈 상태 ② 컬럼 0건 | `?count=3&fail=0` | 처우협의/최종합격/불합격 3컬럼에 "해당 단계 지원자 없음" |
| 재적재 실패 배너 | 기본값 + 재적재 | 배너 + 카드 200장 유지 |

- 스켈레톤 컬럼 수가 `STAGE_ORDER`를 도는지 확인 — 5로 하드코딩하지 않았다.
- `prefers-reduced-motion` 은 빌드된 CSS를 직접 확인:
  `@media (prefers-reduced-motion:reduce){.motion-reduce\:animate-none{animation:none}}`
  → 미디어쿼리가 실제로 산출물에 들어갔다.

**5) 빈 상태 ③(필터 결과 0건)은 이 커밋에서 만들지 않았다 — 의도적**

- 프롬프트는 "자리를 미리 만들어라"였지만, 쓰이지 않는 variant를 넣으면
  그게 바로 죽은 코드다(위 1번과 같은 실수를 반복하는 셈).
- 대신 `EmptyState`를 `title` / `description` / `action`을 받는 **범용 표현**으로만
  만들어 두었다. 커밋 8에서 필터가 생길 때 `action`에 초기화 버튼을 넣어 재사용한다.
  variant 열거 대신 조립 방식이라 미사용 분기가 남지 않는다.

**미검증**: 실제 스크린리더(NVDA 등)에서 `role="alert"`이 즉시 낭독되는지.
DOM에 존재하는 것까지만 확인했다. → 커밋 10에서 접근성 트리로 재확인 예정.

---

## [stage-move] 단계 이동 + API persist

### 프롬프트 1

```
단계 이동을 만든다. 드래그앤드롭 대신 명시적 액션 버튼 방식으로 간다
(과제에서 택1 허용, 이유는 DECISIONS.md에 쓴다).

- 카드에 "단계 이동" 트리거 → 이동 가능한 단계 목록 메뉴. 현재 단계는 disabled.
- 선택하면 moveApplicantStage 호출, 성공 시 상태 갱신.
  새로고침 후에도 유지되는지 확인 가능해야 한다.
- 이번 커밋은 낙관적 업데이트가 아니다. 호출 → 대기 → 성공 시 반영.
  대기 중에는 카드에 pending 표시.
- 리듀서에 MOVE_START / MOVE_SUCCESS / MOVE_FAILURE 추가. 실제 롤백은 다음 커밋.
- expectedVersion으로 현재 카드의 version을 넘겨라.

메뉴는 일단 마우스로 동작하면 된다. 키보드는 a11y 커밋에서 제대로 한다.
```

### AI 출력 요지

`state.pending`(id → true) 추가, 리듀서에 MOVE_START/SUCCESS/FAILURE,
Provider에 `moveStage(id, toStage)` 액션, `StageMoveMenu.tsx`(드롭다운, 현재 단계 disabled,
Esc·외부 클릭으로 닫힘), 카드 레이아웃을 2단(상세 영역 버튼 + 하단 배지/이동 메뉴)으로 변경.

### 리뷰 / 검증

**1) `moveStage`를 `useCallback`에 넣으면서 `state`를 의존성에 넣으면 안 된다**

- **무엇이 문제였나**: `moveStage`는 요청 시 그 카드의 현재 `version`을 알아야 한다.
  가장 자연스러운 코드는 `useCallback((id, stage) => {...}, [state])`인데,
  그러면 상태가 바뀔 때마다 액션 객체가 새로 생성된다.
  → 액션 Context 값이 매번 바뀌고 → **커밋 2에서 Context를 둘로 쪼갠 이유가 사라진다.**
- **어떻게 알아냈나**: 액션을 추가하기 전에 의존성을 어떻게 쓸지 먼저 따져봤다.
  Context 분리의 전제가 "액션 객체가 고정"이라는 걸 커밋 2에서 문서화해둔 덕에
  바로 걸렸다.
- **어떻게 고쳤나**: `stateRef`(useEffect로 갱신)를 두고 이벤트 핸들러에서만 읽는다.
  `moveStage`의 의존성은 빈 배열이 되어 액션 객체가 영구 고정된다.
  render 중 `ref.current = state` 대입이 아니라 `useEffect`를 쓴 이유도 적어뒀다
  (동시성 렌더링에서 render 중 부수효과는 안전하지 않다).

**2) `isPending`을 불리언으로 좁혀서 넘긴 것 — memo 유지의 핵심**

- `<ApplicantCard pending={state.pending} />` 처럼 객체를 넘기면 **어떤 카드든**
  이동할 때마다 `pending` 객체가 새 참조가 되어 카드 1,000장 전부 리렌더된다.
  `isPending={state.pending[id] === true}`로 불리언으로 좁혀 넘겼다.
- 같은 이유로 이동 콜백도 prop으로 내리지 않고 카드가 `useApplicantsActions()`로
  직접 가져오게 했다. 부모가 인라인 화살표 함수를 내려주면 memo가 무력화된다.
- **미검증**: 실제 리렌더 범위. 커밋 6에서 측정한다.

**3) ESLint가 `react-hooks/set-state-in-effect`를 잡았다 — 실제로 고쳤다**

- AI 초안은 "pending이 되면 열려 있던 메뉴를 닫는다"를 이렇게 썼다:
  ```ts
  useEffect(() => { if (isPending) setIsOpen(false) }, [isPending])
  ```
- `npm run lint`가 `react-hooks/set-state-in-effect` 에러로 거부했다.
  린터 말이 맞다 — 이건 상태 동기화이고 렌더를 한 번 더 유발한다.
- **어떻게 고쳤나**: 상태를 동기화하지 않고 **파생값으로 계산**했다.
  ```ts
  const isOpen = isOpenRequested && !isPending
  ```
  effect가 사라졌고 렌더도 한 번 덜 돈다. 린터를 disable 주석으로 막지 않았다.

**4) 브라우저에서 실제로 확인한 것 (가장 중요: persist)**

`?count=200&fail=0`으로 띄우고 첫 카드(윤도희, 서류검토)를 최종합격으로 이동:

| 확인 항목 | 결과 |
| --- | --- |
| 메뉴 항목 | 5개, 현재 단계(`서류검토`)만 `disabled` + "현재" 표시 |
| 요청 중 카드 위치 | **서류검토 그대로** (낙관적 반영이 아니므로 의도한 동작) |
| 요청 중 표시 | 버튼 라벨 `이동 중…`, `disabled: true` |
| 성공 후 카드 위치 | 최종합격 (19명 → 20명) |
| localStorage | `stage: "hired"`, `version: 2`, `stageHistory.length: 2` |

그리고 **새로고침 후 컬럼 건수**:
```
이동 전: 서류검토 89 / 최종합격 19
새로고침 후: 서류검토 88 / 최종합격 20
```
→ mock API가 메모리만 갱신하고 localStorage 쓰기를 빼먹는 흔한 누락이 없음을 확인.
(커밋 1에서 단위 테스트로도 고정해둔 항목을 UI 경로로 다시 확인한 것)

**5) 이번 커밋의 의도적 미완성**

- **실패 시 사용자 피드백이 없다.** `MOVE_FAILURE`는 pending만 해제하고 아무것도
  알려주지 않는다. 낙관적 반영이 없으니 되돌릴 것도 없다. 토스트와 롤백은 커밋 6.
- **연속 클릭을 UI로만 막는다.** pending 중에는 메뉴 버튼이 disabled다.
  하지만 이건 진짜 해결이 아니다 — 액션 자체에는 직렬화가 없어서 프로그램적으로
  빠르게 두 번 호출하면 version이 어긋난다. 커밋 11에서 큐로 해결한다.
- 실패 경로는 브라우저에서 기본 15%를 기다리는 방식으로 확인하려다 시간이 너무 걸려
  중단했다. 커밋 6에서 `?fail=1`로 강제해 검증한다.

---

## [optimistic-update] 낙관적 반영 + 실패 롤백

> 이 과제의 핵심 요구사항이다. 검증에 가장 많은 시간을 썼다.

### 프롬프트 1

```
낙관적 업데이트로 바꾼다. 이 과제의 핵심 요구사항이다.

- 이동 요청 시 UI를 먼저 반영한다. API 응답을 기다리지 않는다.
- API 실패 시 원상 복구 + 사용자 피드백(토스트).
- 롤백 스냅샷은 "mutation 시작 시점의 해당 카드 상태"를 캡처해서
  리듀서 상태의 pendingMoves에 보관해라. 실패 시 그 스냅샷으로 되돌린다.
  중요: 스냅샷을 참조로 들고 있다가 원본이 변형되면 롤백이 깨진다.
  캡처 시점과 불변성 처리를 명확히 해라.
- ConflictError(409)는 단순 롤백이 아니라 서버가 준 현재 상태로 재동기화한다.
  롤백과 재동기화를 코드에서 구분해라.
- 토스트는 실패 사유별로 다르게: 네트워크 실패 / 버전 충돌.
- 낙관적 반영과 서버 확정을 상태에서 구분할 수 있게 해라 (카드에 isPending).

라이브러리 쓰지 마라. 직접 짠다.
```

### AI 출력 요지

액션을 의미별로 4개로 쪼갰다:
`MOVE_OPTIMISTIC`(UI 선반영 + 스냅샷 저장) / `MOVE_CONFIRMED`(서버 객체로 교체) /
`MOVE_ROLLBACK`(스냅샷 복원) / `MOVE_RESYNC`(서버 상태로 맞춤).
`pending: Record<string, true>` → `pendingMoves: Record<string, PendingMove>`로 바꿔
스냅샷과 목표 단계를 함께 보관. 토스트는 별도 `ToastProvider`로 분리.

### 리뷰 / 검증

**1) 스냅샷 캡처 지점 — 얕은 복사가 "우연히" 맞는 것과 "설계로" 맞는 것의 차이**

프롬프트에서 경고했던 부분을 실제로 따져봤다.

- AI는 스냅샷을 `snapshot: current`(복사조차 하지 않은 참조)로 저장했다.
  첫 반응은 "얕은 복사도 아니고 참조라 위험하다"였지만, 따져보니 **이 코드에서는 안전하다.**
  - 롤백 대상은 `state.byId[id]`에 들어 있던 객체 그 자체다.
  - 이 앱은 그 객체를 **절대 변형하지 않는다.** 낙관적 반영도
    `{ ...current, stage: toStage }`로 새 객체를 만들어 교체한다.
  - 즉 스냅샷이 가리키는 객체는 불변이므로 참조 보관이 정확하다.
- **다만 이건 "우연히 맞은" 쪽에 가깝다.** 어딘가에서 `applicant.stage = x` 같은
  직접 변형이 한 번 들어오면 조용히 깨진다. 그래서 두 가지를 했다:
  1. `PendingMove` 타입에 캡처 시점의 의미를 주석으로 못 박았다.
  2. 커밋 7에서 "실패 후 원래 단계 복원"을 테스트로 고정한다 — 회귀 감지 장치.
- **정말 중요한 건 복사 깊이가 아니라 캡처 *순서*였다.** 스냅샷을 낙관적 반영 *이후*에
  캡처하면 롤백이 "방금 낙관적으로 바꾼 값"으로 돌아가 아무 일도 안 하게 된다.
  리듀서 안에서 캡처하면 `state`가 곧 반영 전 상태라 이 순서가 구조적으로 보장된다.
  액션 생성부(Provider)에서 캡처했다면 순서를 사람이 지켜야 했다. 이 점은 채택.

**2) 롤백과 재동기화를 실제로 구분했는지 — 브라우저에서 두 경로를 각각 재현**

`?fail=1`은 초기 로드까지 실패시켜서 "목록은 성공 + 이동만 실패"를 만들 수 없었다.
그래서 개발 모드에서만 mock 서버를 콘솔로 조작할 수 있게 `__mockApi`를 노출했다
(`import.meta.env.DEV` 가드 → **프로덕션 번들에 문자열이 남지 않음을 grep으로 확인**: 0건).

**경로 A — 네트워크 실패 → 롤백**
```
                    강수희 (서류검토)
클릭 직후 40ms:      불합격      ← API 응답(200~800ms) 전에 이미 이동. 낙관적 반영 확인
요청 중:            "이동 중…" 표시 1개
실패 후:             서류검토    ← 롤백 확인
토스트(role=alert): "강수희 님을 불합격(으)로 옮기지 못했습니다
                     서류검토(으)로 되돌렸습니다. 네트워크 오류로…"
```
클릭 후 **40ms 시점**을 찍은 게 핵심이다. mock API 지연이 최소 200ms이므로
이 시점엔 서버가 아직 응답하지 않았다. 그런데 카드가 이미 목표 컬럼에 있다
= UI를 먼저 반영했다는 증거다.

**경로 B — 버전 충돌(409) → 재동기화 (롤백 아님)**

UI를 거치지 않고 서버 상태만 바꿔 클라이언트 version을 낡게 만들었다:
```
__mockApi.moveApplicantStage({id:'apl-00005', toStage:'offer', expectedVersion:1})
→ 서버: stage=offer, version=2   /  화면: 서류검토, version=1 (낡음)
```
그 상태에서 UI로 "최종합격" 이동:
```
이동 전:      서류검토
낙관적 반영:   최종합격
409 처리 후:  처우협의   ← 서류검토(스냅샷)가 아니라 서버 상태로 맞춰졌다
토스트:       "강수희 님의 단계가 이미 변경되었습니다 /
              다른 변경이 먼저 반영되어 처우협의(으)로 맞췄습니다."
```
**롤백이라면 서류검토로 갔을 것이다.** 처우협의로 간 것이 두 경로가 실제로
다르게 동작한다는 증거다. 코드가 두 갈래로 보이는 것만으로는 확인이 안 된다.

**3) `React.memo` 효과 — 커밋 3에서 미룬 검증을 여기서 완료 (대조군까지)**

카드에 렌더 카운터를 임시로 심고 1,000건에서 이동 1건의 렌더 수를 셌다.
그리고 **memo를 떼고 같은 측정을 반복**해 대조군을 만들었다.

| | 초기 로드 | 낙관적 이동 1건 | 서버 확정 |
| --- | --- | --- | --- |
| `memo` 적용 | 2,000 | **2** | 2 |
| `memo` 제거 | 2,000 | **2,000** | 2,000 |

(StrictMode가 렌더를 2회 호출하므로 실제 카드 수의 2배로 나온다. 즉 memo 적용 시
이동 1건에 리렌더되는 카드는 **1장**이다.)

→ 1,000배 차이. memo가 "붙어 있기만" 한 게 아니라 실제로 동작한다.
계측 코드와 memo 제거는 측정 후 모두 원복했다(`grep`으로 잔존 0건 확인).

이 측정 덕에 커밋 5에서 판단했던 두 가지가 근거를 얻었다:
- `isPending`을 객체가 아닌 불리언으로 좁혀 넘긴 것
- 이동 콜백을 prop으로 내리지 않고 카드가 액션 Context에서 직접 가져오는 것

둘 중 하나만 어겼어도 위 표의 "2"는 "2,000"이 됐을 것이다.

**4) 토스트를 별도 Provider로 분리한 판단**

- 지원자 스토어에 `toasts` 배열을 넣으면 토스트 하나 뜰 때마다 스토어 상태가 바뀌어
  보드 전체가 리렌더된다. 위 3번 측정이 무의미해진다.
- 토스트 Provider 안에서도 **목록 Context와 API Context를 나눴다.**
  `push`만 쓰는 스토어가 토스트 목록 변경에 리렌더되면 안 되기 때문이다.
- live region 컨테이너는 **항상 DOM에 두고** 내용만 갈아끼운다.
  토스트와 함께 컨테이너를 마운트하면 스크린리더가 안 읽는 흔한 실수를 피하려는 것.
  실패는 `role="alert"`, 성공은 `role="status"`로 나눴다.

**5) 내 검증 스크립트가 틀렸던 일 (기록용)**

메뉴가 안 열린다고 3번 헤맸다. 원인은 앱이 아니라 내 스크립트였다 —
직전 스크립트가 남긴 "열린 상태"를 다음 스크립트가 토글해서 닫고 있었다.
`aria-expanded`를 시점별로 찍어보니 클릭 후 마이크로태스크에 이미 `true`였다.
멱등적으로 여는 헬퍼(`aria-expanded !== 'true'`일 때만 클릭)로 바꾼 뒤 해결.
→ 커밋 3의 `clientHeight: 8`과 같은 교훈이 반복됐다. **측정이 이상하면 측정을 먼저 의심.**

**미검증 (다음 커밋으로)**
- 같은 카드를 빠르게 연속 이동했을 때. 지금 구현은 `MOVE_OPTIMISTIC`이 오면
  **기존 스냅샷을 덮어쓴다.** 두 번째 이동의 스냅샷은 "첫 이동의 낙관적 상태"이므로
  실패 시 원래 단계가 아닌 중간 상태로 돌아간다. → 커밋 7에서 테스트로 드러내고 11에서 고친다.
- 늦게 도착한 응답이 최신 상태를 덮어쓰는 문제. 이동 경로에는 아직 시퀀스 가드가 없다.
  (초기 로드에는 커밋 2에서 넣었다) → 동일.

---

## [test-optimistic] 낙관적 업데이트 / 롤백 / 순서 테스트

> **이 커밋은 테스트 2개가 빨간 상태로 들어간다.** 의도한 것이다.

### 프롬프트 1

```
낙관적 업데이트와 롤백을 테스트한다. Vitest + RTL.

mock API의 지연/실패확률은 테스트에서 주입할 수 있게 만들어뒀다.
그걸 써서 결정론적으로 짜라. Math.random 몽키패치 금지.

케이스:
1) 이동 요청 직후, API가 아직 응답하지 않은 시점에 UI가 이미 목표 단계에 있다
2) API 실패 시 원래 단계로 롤백되고 에러 피드백이 노출된다
3) 같은 카드를 빠르게 연속 이동시켜도 최종 상태가 마지막 의도와 일치한다
4) 늦게 도착한 응답(stale)이 최신 상태를 덮어쓰지 않는다
5) ConflictError 시 서버 상태로 재동기화된다 (단순 롤백이 아니다)

3번과 4번은 아직 구현이 없으니 실패할 거다.
실패하는 테스트를 그대로 남기고 왜 실패하는지 알려줘라.
it.fails나 todo로 숨기지 마라 — race-condition 커밋에서 고친다.
```

### AI 출력 요지

`reducer.test.ts`(리듀서 단위 10개), `test/harness.tsx`(보드+스토어+토스트 실렌더 + 상태/액션
프로브), `optimisticMove.test.tsx`(통합 6개). `moveApplicantStage`를 요청마다 수동
resolve/reject 가능한 deferred로 대체해 응답 도착 순서를 뒤집을 수 있게 했다.

### 리뷰 / 검증

**1) 프롬프트의 지시를 일부 따르지 않았다 — 이유가 있다**

- 나는 "mock API의 지연/실패 주입을 써라"라고 지시했는데, AI는 `vi.mock`으로
  `moveApplicantStage`를 **deferred로 대체**했다. 처음엔 지시 위반으로 보였다.
- 따져보니 AI가 맞다. 커밋 1의 주입 창구로는 `failureRate`와 지연 *범위*만 통제된다.
  4번 케이스("늦게 도착한 응답")는 **두 요청의 응답 도착 순서를 뒤집어야** 하는데,
  랜덤 지연으로는 순서를 결정론적으로 만들 수 없다.
- **채택.** 단 `ConflictError`/`NetworkError`는 실제 클래스를 그대로 쓰게 했다.
  Provider가 `instanceof ConflictError`로 분기하므로 가짜 객체면 테스트가 통과해도
  프로덕션 경로를 검증하지 못한다. (`importOriginal`로 나머지는 실제 구현 유지)
- 커밋 1의 주입 창구는 그쪽 파일(`mockApi.test.ts`)에서 실제로 쓰고 있으므로 헛일이 아니다.

**2) 이동을 메뉴 클릭이 아니라 액션 호출로 일으킨 판단**

- 3번 케이스(빠른 연속 이동)는 **UI로는 재현되지 않는다.** pending 중에 메뉴 버튼이
  disabled이기 때문이다.
- 그 UI 잠금은 완화책일 뿐 경쟁 상태의 해결이 아니다. 잠금을 우회해서 스토어 로직
  자체를 검증해야 한다고 판단해 프로브로 `moveStage`를 직접 호출했다.
- 대신 결과 확인은 **DOM에서 읽는다**(`columnOf`가 카드가 실제로 어느 `section`
  안에 있는지 조회). 상태만 보면 "리듀서는 맞았는데 화면은 안 바뀐" 경우를 놓친다.

**3) 1번 케이스가 진짜로 "응답 전"을 보는지 확인했다**

- `await waitFor(...)`로 감싸면 응답 *후*를 보게 되어 테스트가 무의미해진다.
  그래서 `act()` 동기 블록 안에서 이동을 일으킨 뒤, **deferred가 아직 미해결임을
  먼저 단정**(`expect(moveCalls).toHaveLength(1)`)하고 그다음 화면을 확인한다.
  서버가 응답하지 않았다는 게 테스트 안에서 증명된다.

**4) 3번·4번이 왜 실패하는지 (실제 실패 메시지)**

```
FAIL 3) 같은 카드 빠른 연속 이동 > 최종 상태가 마지막 의도와 일치한다
  expected 'interview' to be 'offer'

FAIL 4) 늦게 도착한 응답 > 낡은 응답이 최신 상태를 덮어쓰지 않는다
  expected 'interview' to be 'offer'
```

원인 두 가지:
- **3번**: 두 요청이 병렬로 나가고 **둘 다 `expectedVersion: 1`**을 들고 간다
  (두 번째 호출 시점에 서버 확정이 없어 로컬 version이 그대로이므로).
  첫 요청이 성공해 서버 version이 2가 되면 두 번째는 409로 거부된다.
  → 마지막 의도(`offer`)가 유실되고 `interview`로 끝난다.
- **4번**: `MOVE_CONFIRMED`에 순서 개념이 없다. 늦게 온 첫 요청의 응답이
  나중에 도착해 최신 상태를 덮어쓴다.
- 추가로 발견: **스냅샷도 덮어쓰인다.** 두 번째 `MOVE_OPTIMISTIC`이 첫 이동의
  낙관적 상태(`interview`)를 스냅샷으로 저장하므로, 실패 시 원래 단계가 아니라
  중간 상태로 롤백된다. 이건 커밋 6에서 "미검증"으로 적어둔 항목이 확인된 것이다.

**5) 실패를 숨기지 않은 이유**

`it.skip` / `it.todo` / `it.fails`로 감싸면 "구현이 아직 없다"와 "테스트를 안 썼다"가
커밋 히스토리에서 구별되지 않는다. 빨간 상태로 커밋하면 무엇이 미해결인지가
기록에 남고, 커밋 11에서 초록으로 바뀌는 diff가 곧 해결의 증거가 된다.

**6) 플레이키 의심 → 측정으로 기각**

- 최초 실행에서 실패 4개가 나왔다가 다음 실행에서 2개가 나왔다.
  테스트가 불안정하면 아무것도 검증할 수 없으므로 먼저 확인했다.
- `npm run test`를 **5회 연속** 돌려 전부 `2 failed | 33 passed`로 동일함을 확인.
  최초 4개는 콜드 트랜스폼 캐시 상태의 일회성 결과였다.

**7) `stateRef` 지연 의혹 → 테스트로 찔러보고 기각**

- 커밋 5에서 `moveStage`가 `stateRef`(useEffect로 갱신)에서 version을 읽게 했다.
  ref가 한 커밋 뒤처지면 **확정 직후의 이동이 낡은 version을 보내 헛된 409**가 난다.
  실제로 그런지 추측하지 않고 테스트를 하나 더 썼다:
  ```
  이동 → version 1로 요청 → 서버가 version 2로 확정 → 곧바로 다시 이동
  expect(moveCalls[1].input.expectedVersion).toBe(2)  // PASS
  ```
- **통과했다.** 이벤트 핸들러는 커밋+effect 이후에 실행되므로 ref가 항상 최신이다.
  설계는 그대로 두고, 이 테스트를 회귀 감지 장치로 남겼다.

**8) ESLint가 두 번 막았다**

- `importOriginal<typeof import('../../mocks')>()` →
  `@typescript-eslint/consistent-type-imports`가 `import()` 타입 주석을 금지.
- 타입 별칭으로 우회해도 같은 규칙에 걸렸다. 최종적으로
  `import * as mocksModule`(정적) + `importOriginal<typeof mocksModule>()`로 해결했다.
  `vi.mock`이 import보다 위로 호이스팅되므로 정적 import도 대체된 모듈을 가리킨다는
  점을 확인하고 주석에 남겼다. 규칙을 disable하지 않았다.

**현재 상태**: `34 passed | 2 failed (36)` — 실패 2개는 3번·4번.

---

## [search-filter] 이름 검색 + 직무 필터

### 프롬프트 1

```
검색/필터를 만든다. 데이터 1,000건에서도 체감 지연이 없어야 한다.

- 이름 검색(부분일치, 대소문자/공백 무시) + 직무 필터(다중선택)
- 디바운스 대신 useDeferredValue를 써라.
  입력 반응성은 유지하고 목록 계산만 지연시킨다.
- 검색 대상 문자열은 미리 정규화해서 캐싱해라 (매 렌더마다 toLowerCase 하지 마라).
- 컬럼별 필터 결과는 useMemo 셀렉터로. 조건이 안 바뀌면 재계산 안 되게.
- 활성 필터 표시 + 초기화 버튼. 결과 0건이면 커밋 4의 빈 상태를 쓴다.
- 필터를 URL 쿼리스트링에 반영하지 않아도 된다. 대신 왜 안 했는지 알려줘라.

1000건 모드에서 입력 지연을 실측할 수 있게 해줘.
```

### AI 출력 요지

`lib/search.ts`(정규화 1곳), `features/filters/FiltersProvider.tsx`(필터 상태, 별도 Context),
`FilterBar.tsx`(검색 입력 + 직무 체크박스 팝오버 + 초기화), 셀렉터를
`groupByStage` → `selectBoardView(state, filters)`로 교체(분류와 필터를 1회 순회로 동시 처리),
리듀서 `LOAD_SUCCESS`에서 `searchIndex`(id → 정규화 문자열) 구축.

### 리뷰 / 검증

**1) 정규화 규칙을 한 곳으로 모은 이유 — 반쪽 동작 방지**

- "공백 무시"는 **인덱스 쪽과 질의어 쪽이 같은 규칙**을 써야 성립한다.
  한쪽만 공백을 지우면 `"kim alex"` 검색이 실패한다.
- `normalizeSearchText` 하나를 양쪽에서 쓰게 하고, 그 사실을 테스트로 고정했다:
  `'kim alex'` / `'kimalex'` / `' k i m '` 세 형태 모두 1건을 찾는지 확인 → 통과.

**2) 인덱스를 "언제" 만드는지가 핵심 — `useMemo`로는 부족하다**

- AI 초안은 처음에 셀렉터 안에서 매번 `toLowerCase`를 돌리려 했다.
  `useMemo`로 감싸도 **필터 조건이 바뀔 때마다** 1,000건을 다시 정규화한다
  (의존성에 filters가 들어가므로). 타이핑 한 글자마다 1,000회 문자열 연산이다.
- `LOAD_SUCCESS` 시점에 `searchIndex`를 한 번 만들어 상태에 보관하게 바꿨다.
  이름·직무는 이동으로 바뀌지 않으므로 로드 때 한 번이면 충분하다.
- 직무 필터도 `Array.includes`(O(n)) 대신 `Set`으로 바꿨다. 직무 여러 개를 고르면
  건수 × 직무 수가 되기 때문이다.

**3) `useDeferredValue` vs 디바운스 — 지시대로 하되 이유를 확인했다**

- 디바운스는 "일정 시간 아무 일도 안 하는" 방식이라 (a) 지연 시간을 사람이 추측해서
  골라야 하고 (b) 빠르게 치면 중간 결과가 전혀 안 보이고 (c) 멈추면 한 박자 늦게 튄다.
- `useDeferredValue`는 입력을 즉시 반영하고 무거운 목록만 이전 값으로 버틴다.
  `deferredFilters !== filters`로 "아직 따라잡는 중"을 알 수 있어서, 그 동안
  보드를 살짝 흐리게 처리했다(계산 중임을 사용자가 알게).

**4) 1,000건 입력 지연 실측 — dev 수치를 그대로 믿지 않았다**

`?count=1000`에서 "백엔드 개발"을 한 글자씩 입력하며 각 키 입력의 동기 커밋 시간을 측정.

| 빌드 | 키 입력별(ms) | 최대 |
| --- | --- | --- |
| 개발 서버 | 35.1 / 34.2 / 31.2 / 37.2 / 10.7 / 1.1 | **37.2ms** |
| 프로덕션 빌드(`vite preview`) | 4.4 / 2.0 / 2.7 / 0.3 / 0.5 / 0.3 | **4.4ms** |

- 개발 서버 수치(37ms)는 프레임 예산(16ms)을 넘는다. 여기서 멈추면
  "느리다"고 잘못 결론 낼 수 있었다. **StrictMode의 2배 렌더 + 미압축 코드**가
  섞인 값이기 때문이다.
- 프로덕션 빌드로 다시 재니 최대 4.4ms, 대부분 3ms 미만이었다.
  즉 실사용 조건에서는 프레임 예산 안에 들어온다.
- 뒤쪽 글자에서 시간이 급감하는 건(0.3ms) 필터가 좁혀져 렌더할 카드가 줄기 때문이다.

**5) 필터 정확성 — 화면에서 확인**

```
"백엔드 개발" 입력 → 119 / 1,000명
컬럼: 52 + 26 + 16 + 13 + 12 = 119  ✓ (합계 일치)
```
빈 상태 ③:
```
"존재하지않는이름xyz" → "조건에 맞는 지원자가 없습니다 / 검색어나 직무 필터를 조정해 보세요."
                        + 필터 초기화 버튼 (요약줄과 빈 상태에 각 1개, 총 2개)
초기화 클릭 → 카드 1,000장 복귀, 입력값 '', 컬럼 445/250/115/84/106
```

**6) 내 테스트가 틀렸다 — 로케일 정렬 순서를 가정한 것**

- `selectPositions`의 결과를 `['DevOps', '백엔드 개발', '프론트엔드 개발']`로 단정했는데 실패:
  ```
  expected [ '백엔드 개발', '프론트엔드 개발', 'DevOps' ]
  to deeply equal [ 'DevOps', '백엔드 개발', '프론트엔드 개발' ]
  ```
- 원인: `localeCompare(a, b, 'ko')`는 이 런타임에서 **한글을 라틴 문자보다 앞**에 놓는다.
  내가 라틴 우선이라고 가정한 것이 틀렸다.
- **구현을 고치지 않았다.** `Intl` 정렬은 런타임의 ICU 데이터에 의존해서
  Node와 브라우저가 다를 수 있다. 순서에 의존하는 단정 자체가 깨지기 쉬운 테스트다.
  검증해야 하는 성질은 "중복 없음"과 "실행마다 동일함"이므로 그렇게 다시 썼다.

**7) 필터를 URL에 반영하지 않은 이유 (지시대로 답)**

이 화면은 새로고침이 곧 mock 서버 재적재라서 URL 공유의 실익이 적고,
`?count`/`?fail` 같은 검증용 파라미터와 섞이면 오히려 혼란스럽다.
필터는 세션 내 화면 조건으로만 두는 게 이 과제 범위에 맞다고 판단했다.
(실제 서비스라면 공유 가능한 URL이 맞다 — 이 결정은 범위 판단이다)

**현재 상태**: `45 passed | 2 failed (47)` — 실패는 여전히 커밋 11에서 해결할 3·4번뿐.

---

## [detail-panel] 지원자 상세 사이드 패널

### 프롬프트 1

```
카드 클릭 시 지원자 상세를 사이드 패널로 띄운다.

- 오른쪽 슬라이드인 패널. 모달 아니고 패널.
  (이유: 보드 문맥을 유지한 채 볼 수 있다 — DECISIONS.md에 쓴다)
- 표시: 이름, 직무, 지원일, 현재 단계, 이메일/연락처, 경력연수,
  이력요약, 메모, 그리고 단계 이동 히스토리
- 패널에서도 단계 이동 가능. 커밋 6의 낙관적 업데이트 로직을 그대로 재사용해라.
  복붙하지 마라.
- 상세는 getApplicant로 개별 조회한다. 이것도 15% 실패하니
  패널 안에 로딩/에러/재시도 처리가 필요하다.
- Esc로 닫기, 열릴 때 포커스 이동, 닫으면 원래 카드로 포커스 복귀.
  role="dialog" aria-modal 처리.
- 패널이 열린 카드가 낙관적 이동 중이면 패널에도 pending이 보여야 한다.
```

### AI 출력 요지

`SelectionProvider`(선택된 id + open/close, 값/API Context 분리),
`lib/useFocusTrap.ts`, `features/detail/ApplicantDetailPanel.tsx`,
리듀서에 `APPLICANT_FETCHED` 추가. 패널은 스토어만 읽고 자기 사본을 갖지 않는다.
배경 보드는 패널이 열리면 `inert` 처리.

### 리뷰 / 검증

**1) `aria-modal="true"`와 "패널이라 문맥 유지" 사이의 모순을 해결한 방식**

- `aria-modal="true"`를 선언하면 보조기술은 "이 대화상자 밖은 없는 것"으로 취급한다.
  그런데 포커스가 뒤 보드로 새어나가면 그 선언이 거짓말이 된다.
- 반대로 전체 모달로 만들면 "보드 문맥을 유지한다"는 패널 선택 이유가 사라진다.
- **`inert` 속성**으로 풀었다. 배경 보드는 **시각적으로는 그대로 보이면서**
  포커스·클릭·접근성 트리에서만 빠진다. 선언과 실제 동작이 일치하고
  패널을 택한 이유도 유지된다. 여기에 포커스 트랩을 더해 이중으로 막았다.

**2) 포커스 복귀가 `<body>`로 떨어졌다 — inert와 렌더 타이밍이 얽힌 실제 버그**

- **증상**: 패널을 닫으니 포커스가 원래 카드가 아니라 `<body>`로 갔다.
  ```
  { dialogClosed: true, focusAfterClose: "BODY" }   ← 기대: "윤도희 상세 보기"
  ```
- **원인**: AI 초안(그리고 가장 자연스러운 코드)은 `close()` 안에서
  `setSelectedId(null)` 직후에 `opener.focus()`를 호출했다.
  그 시점에는 아직 리렌더가 일어나지 않아 **배경 보드가 여전히 `inert`** 상태다.
  그리고 **inert 서브트리 안의 요소는 `focus()` 호출이 무시된다.**
  결과적으로 포커스가 아무 데도 못 가고 `<body>`로 떨어진다.
- **어떻게 알아냈나**: 브라우저에서 실제로 열고 닫은 뒤 `document.activeElement`를 찍었다.
  코드만 읽으면 "닫고 포커스 되돌리기"가 순서대로 잘 쓰인 것처럼 보인다.
- **어떻게 고쳤나**: 포커스 복귀를 `useLayoutEffect`로 옮겼다.
  `selectedId`가 null이 된 **뒤**, 즉 DOM에서 `inert`가 제거된 다음에 실행된다.
  `useEffect`가 아니라 `useLayoutEffect`인 이유는 페인트 전에 끝내서
  포커스 링이 한 프레임 튀는 걸 막기 위해서다.
- **재검증**: `focusReturnedTo: "윤도희 상세 보기"` — 원래 카드로 복귀 확인.

**3) 내 첫 Tab 트랩 테스트는 무의미했다 (합성 이벤트의 한계)**

- 처음엔 `new KeyboardEvent('keydown', {key:'Tab'})`을 dispatch해서 확인했다.
  결과가 "포커스가 패널을 벗어나지 않음"으로 나왔지만 이건 **증명이 아니다** —
  합성 이벤트는 리스너만 호출하고 브라우저의 기본 동작(포커스 이동)을 하지 않는다.
  포커스가 움직이지 않았으니 당연히 안 벗어난 것이다.
- **실제 키 입력**(자동화 도구의 키 이벤트)으로 Tab을 6번 눌러 다시 확인:
  `afterRealTabs_focusInsidePanel: true` — 이번엔 진짜 검증이다.

**4) 실제 Escape 키는 검증하지 못했다 — 원인을 확인하고 한계로 기록**

- 실제 Escape 키를 눌렀는데 패널이 닫히지 않았다. 앱 버그를 의심했다.
- **계측**: `document`에 keydown 프로브를 달고 실제 Escape를 눌렀다.
  ```
  { keyEventsSeenByPage: [], dialogStillOpen: true }
  ```
  → **페이지에 keydown 이벤트가 단 하나도 도달하지 않았다.**
  브라우저 자동화 패널이 Escape를 자체적으로 삼킨다. Tab은 도달했으므로
  키 입력 경로 자체가 막힌 건 아니다.
- 동일한 버블링 경로(포커스된 요소에서 dispatch)로 Escape를 보내면 정상 동작한다:
  `dialogClosedBySyntheticEsc: true`, `inertRemoved: true`, 포커스 복귀까지 확인.
- **정직하게 남긴다: 실제 Escape 키는 자동화로 검증 불가.**
  코드 경로는 동일하지만 브라우저에서 손으로 한 번 눌러 확인할 항목이다.

**5) 패널이 자기 사본을 갖지 않게 한 것 — 실제로 어긋나지 않는지 확인**

가장 걱정한 부분이다. 패널이 `getApplicant` 결과를 자기 state에 넣으면
낙관적 이동이 일어났을 때 보드와 패널의 단계가 달라진다.
그래서 조회 결과를 `APPLICANT_FETCHED`로 **스토어에 흘려보내고** 패널은 스토어만 읽게 했다.

패널을 열어둔 채 이동을 강제 실패시켜 확인:
```
이동 전:        패널 서류검토
낙관적 반영:    패널 불합격, "이동 중" 표시 있음
실패 후:        패널 서류검토  (보드도 서류검토)
토스트:         "윤도희 님을 불합격(으)로 옮기지 못했습니다 / 서류검토(으)로 되돌렸습니다"
```
패널과 보드가 같은 값을 따라간다 = 사본이 없다는 증거.

**6) 상세 조회가 낙관적 상태를 되돌릴 수 있는 문제 — 리듀서에서 막았다**

- 상세 조회는 이동 요청과 무관하게 날아간다. 서버가 아직 이동을 반영하지 않은
  조회 응답이 늦게 도착하면 화면의 낙관적 상태를 되돌려 버릴 수 있다.
- `APPLICANT_FETCHED`는 **해당 카드에 진행 중인 이동이 있으면 응답을 버린다.**
  이동이 확정되면 `MOVE_CONFIRMED`가 서버 객체 전체(`stageHistory` 포함)로
  교체하므로 데이터는 어차피 최신이 된다. 병합 로직을 만들지 않은 이유다.
- 패널 안에도 조회 순번(`fetchSeq`) 가드를 넣어 재시도 시 낡은 응답을 버린다.

**7) 이동 로직 재사용 — 복붙 없음**

패널은 `StageMoveMenu`를 그대로 쓰고 `moveStage`를 그대로 호출한다.
패널 전용 이동 코드는 없다. (프롬프트에서 "복붙하지 마라"고 지시한 부분)

**8) ESLint가 세 번째로 잡았다**

- `useEffect(() => { loadDetail() })`에서 `loadDetail`이 동기적으로
  `setFetchStatus('loading')`을 호출 → `react-hooks/set-state-in-effect`.
- 초기값을 `'loading'`으로 두고, effect는 **요청만 시작**하게 분리했다.
  상태 전이는 응답 콜백(effect 밖)과 재시도 버튼(이벤트 핸들러)에서만 일어난다.
  렌더도 한 번 줄었다.

**미검증**
- 실제 Escape 키 (위 4번, 자동화 한계)
- 스크린리더에서 `aria-modal` + `inert` 조합이 의도대로 읽히는지 → 커밋 10에서 접근성 트리로 확인

---

## [a11y-keyboard] 키보드 접근성

### 프롬프트 1

```
키보드만으로 전부 조작 가능하게 만든다. 과제 요구사항이다 —
단계 이동과 상세 열람이 키보드로 가능해야 한다.

- 카드 간 이동: 방향키. ←→ 컬럼 간, ↑↓ 같은 컬럼 내.
  roving tabindex로 컬럼당 Tab 스톱 하나.
- Enter/Space: 상세 패널 열기
- 단계 이동 메뉴: 카드 포커스 상태에서 M 키 또는 메뉴 버튼.
  메뉴는 방향키 탐색 + Enter 선택 + Esc 취소.
- aria-live polite 영역:
  "홍길동 님을 면접 단계로 이동했습니다" /
  "이동 실패, 서류검토로 되돌렸습니다"
- 컬럼에 aria-label로 단계명 + 건수
- 포커스 링 전부 보이게. outline: none 쓴 곳 있으면 고쳐라.
- 키보드 단축키 안내를 화면에 노출 (또는 ? 키)

스크린리더 없이도 검증할 방법을 같이 알려줘라.
```

### AI 출력 요지

`useBoardKeyboardNav`(방향키 + roving tabindex), `AnnouncerProvider`(live region 단일 창구),
`StageMoveMenu`에 `useImperativeHandle`로 `open()` 노출(카드의 M 키가 호출),
메뉴 내부 ↑↓/Enter/Esc, `KeyboardHelp`(헤더 버튼 + `?` 키), 카드 `aria-label` 보강.

### 리뷰 / 검증

**1) 브라우저 자동화로는 키보드를 검증할 수 없었다 — 원인을 밝히고 방법을 바꿨다**

- 실제 방향키를 눌렀는데 포커스가 안 움직이고 **페이지가 스크롤**됐다(`scrollY: 422`).
  `preventDefault`가 안 걸린 것처럼 보여 코드를 의심했다.
- **계측**: `document`에 캡처 단계 keydown 프로브를 달고 실제 방향키를 눌렀다.
  ```
  { captured: [], focusNow: (변화 없음), scrollY: 0 }
  ```
  → **keydown이 페이지에 단 한 건도 도달하지 않았다.** Escape도 마찬가지였다.
  Tab만 전달됐다(그래서 커밋 9의 포커스 트랩은 실제 키로 검증할 수 있었다).
  브라우저 자동화 패널이 방향키/Escape를 자체 단축키로 삼킨다.
- **판단**: 이 환경에서 "실제 키로 확인"은 불가능하다. 그렇다고 미검증으로 남기면
  요구사항의 핵심(키보드만으로 조작)을 아무도 확인하지 않은 채 제출하게 된다.
  → **jsdom + `@testing-library/user-event`로 옮겼다.** 회귀 감지까지 얻는다.
- 결과: 키보드 테스트 **16개 전부 통과**.

**2) 키보드 테스트가 실제로 검증하는 것**

| 항목 | 확인 내용 |
| --- | --- |
| roving tabindex | 카드 6장 중 Tab 스톱은 **비어 있지 않은 컬럼 수(3)만큼** |
| 포커스 이동 시 스톱 갱신 | 이동 후에도 컬럼당 1개 유지, 이전 카드는 `tabindex="-1"` |
| ↓ ↑ | 같은 컬럼 내 이동, **양 끝에서 멈춤**(다음 컬럼으로 넘어가지 않음) |
| → | 같은 행 위치 유지 (`서류이`(index 1) → `면접이`(index 1)) |
| → 빈 컬럼 | 처우협의(0건)를 **건너뛰고** 최종합격으로 |
| → 행 초과 | 대상 컬럼이 더 짧으면 마지막 카드로 붙음 |
| Home / End | 컬럼 처음 / 끝 |
| Enter | 상세 패널 열림, `aria-modal="true"`, 접근 이름이 지원자 이름 |
| 닫기 | **원래 카드로 포커스 복귀** (커밋 9의 버그 회귀 방지) |
| M → ↓ → Enter | 메뉴 열림 → 첫 활성 항목에 포커스 → 선택 시 실제로 단계 이동 |
| 메뉴 Escape | 메뉴 닫힘 + **트리거 버튼으로 포커스 복귀** |
| live region | polite/assertive 둘 다 **처음부터 DOM에 존재** |
| 이동 안내 | 확정 시 polite 영역에 "…님을 면접 단계로 이동했습니다" |
| 컬럼 이름 | `region` 역할 + "서류검토 3명" / "처우협의 0명" |

브라우저에서 추가로 확인한 것: 200건에서 **카드 내부 Tab 스톱이 10개**
(5컬럼 × 2 = 상세 버튼 + 이동 버튼), 전체 Tab 가능 요소 14개.
roving tabindex가 없으면 400개가 됐을 것이다.

**3) live region을 토스트에서 떼어내 단일 창구로 만든 판단**

- 커밋 6에서 `ToastViewport`에 `aria-live`를 달고 토스트마다 `role="alert"`을 줬다.
  이번에 안내 문구를 추가하면서 문제가 보였다 — **성공한 이동은 토스트를 띄우지 않는데
  안내는 해야 한다.** 그리고 커밋 12에서 Undo 토스트가 생기면 같은 사건이
  토스트와 live region에서 **두 번 낭독**된다.
- 안내를 `AnnouncerProvider` 하나로 모으고, `ToastViewport`에서는 `aria-live`와
  `role`을 **제거**했다. 토스트는 시각 표현만 담당한다.
- 부작용: 커밋 7의 롤백 테스트가 `findByRole('alert')`로 토스트를 찾고 있어서 깨졌다.
  **테스트를 새 채널(assertive live region)로 고치고**, 시각 토스트도 함께 뜨는지
  별도로 단정하도록 보강했다. 채널을 바꿨으면 검증도 따라가야 한다.
- 같은 문구가 연속으로 오면 텍스트가 안 바뀌어 낭독되지 않는 문제도 처리했다
  (빈 문자열을 한 번 거쳐 변경을 확실히 만든다).

**4) 카드 `aria-label`에서 단계 배지를 `aria-hidden` 처리**

카드 버튼의 접근 이름에 이미 단계가 들어간다
(`"윤도희, 백엔드 개발, 서류검토, 2026.07.31 지원. 상세 보기"`).
배지를 그대로 두면 스크린리더가 단계를 두 번 읽으므로 `aria-hidden="true"`로 뺐다.
브라우저에서 접근 이름을 직접 읽어 확인했다.

**5) ESLint / React Compiler가 이 커밋에서 네 번 막았다 — 전부 규칙을 따랐다**

1. `location` 파생값의 `useMemo` → *"기존 메모이제이션을 보존할 수 없다"*.
   콜백 안의 early return 때문이었다. **메모를 없앴다** — 단계 5회 루프라
   비용이 없고, 결과는 카드에 불리언으로 좁혀 넘기므로 참조가 바뀌어도
   카드 memo에 영향이 없다. (규칙을 끄면 컴파일러 최적화 전체가 꺼진다)
2. `tabStopByStage`의 `useMemo` → 같은 이유. 역시 제거.
3. 테스트 harness의 `Capture`가 렌더 중 바깥 변수에 대입 →
   `Cannot reassign variables declared outside of the component/hook`.
   객체 프로퍼티로 바꿨더니 이번엔 `react-hooks/immutability`.
   → **effect로 옮겼다.** 렌더는 순수하게 두고 커밋 이후에만 값을 내보낸다.
   테스트 단정은 어차피 effect 플러시 뒤라 동작 차이가 없다.
4. (커밋 5·9에서도 `set-state-in-effect`로 두 번 막혔다)

네 번 모두 `eslint-disable`을 쓰지 않았다. 규칙이 지적한 게 실제로
불필요한 렌더나 순수성 위반이었다.

**6) 내 첫 harness 수정이 조용히 실패했던 일**

패널을 harness에 추가했는데 테스트가 계속 실패했다. 원인은 prettier가
JSX 들여쓰기를 바꿔놓아서 **문자열 치환 대상이 안 맞았고, 치환이 조용히 넘어간 것**이었다.
렌더 트리를 직접 출력해 `<Board />`가 그대로인 걸 보고 알았다.
"고쳤는데 안 된다"면 고쳐졌는지부터 확인해야 한다.

**미검증**
- 실제 스크린리더(NVDA/VoiceOver) 낭독. 접근성 트리의 이름·역할·live region 존재까지만
  확인했다. 낭독 순서와 억양은 실기기에서 들어봐야 한다.
- 실제 방향키/Escape 키 입력 (위 1번, 자동화 한계). jsdom에서 동일 경로를 검증했다.

---

## [race-condition] 카드별 요청 직렬화 + 버전 충돌

### 프롬프트 1

```
커밋 7에서 실패로 남겨둔 테스트 3, 4번을 통과시킨다. 경쟁 상태 처리다.

문제: 같은 카드를 빠르게 연속 이동하면
(a) 요청이 병렬로 나가 서버 version이 어긋나고
(b) 늦게 온 응답이 최신 낙관적 상태를 덮어쓴다

- 카드별 요청 직렬화: 카드 id 단위 큐. 같은 카드는 순차 실행,
  다른 카드는 병렬 유지.
- 연속 클릭 병합: 큐에 대기 중인 같은 카드의 이동은 마지막 의도만 남긴다.
  단, 이게 stageHistory에 미치는 영향을 판단해서 알려줘라.
- 응답에 시퀀스 번호를 붙이고, 카드의 현재 시퀀스보다 낮은 응답은 무시.
- 롤백 스냅샷은 "큐 시작 시점" 기준이어야 한다.
  중간 낙관적 상태로 롤백하면 안 된다.
- version 충돌 시 재시도 정책을 정해라. 무한 재시도 하지 마라.

큐 로직은 리듀서 밖 별도 모듈로 분리하고 단위 테스트 가능하게 해라.
```

### AI 출력 요지

`features/applicants/moveQueue.ts`(React 비의존 순수 모듈), 리듀서
`MOVE_OPTIMISTIC`에서 **기존 스냅샷 유지**, Provider가 큐를 effect에서 생성해 연결,
`StageMoveMenu`의 pending 잠금 해제, 큐 단위 테스트 14개.

### 리뷰 / 검증

**1) version을 스토어에서 읽으면 안 된다는 걸 설계 단계에서 잡았다**

- 가장 자연스러운 구현은 요청 직전에 `stateRef.current.byId[id].version`을 읽는 것이다.
  그런데 큐는 **응답 직후 곧바로 다음 요청을 보낸다.** 그 시점에는 React가 아직
  커밋하지 않아 스토어를 읽으면 낡은 version이 나오고, 두 번째 요청이 무조건 409가 된다.
- 그래서 큐가 **자기 안에 version을 들고 간다.** 첫 진입 시 스토어에서 한 번 읽고,
  이후에는 각 응답의 version으로 갱신한다. React 타이밍 의존이 사라진다.
- 다만 큐는 **다 처리하면 스스로 비운다.** 무기한 들고 있으면 다른 경로(상세 조회,
  전체 재적재)로 바뀐 서버 상태를 영원히 모르게 되기 때문이다.
  다음 이동은 스토어에서 다시 읽는다.
  → 이 부분에서 내 테스트가 먼저 틀렸다(아래 5번).

**2) 롤백 스냅샷 문제 — 커밋 6의 "미검증" 항목이 해결됐다**

커밋 6에 이렇게 적어뒀었다: *"`MOVE_OPTIMISTIC`이 오면 기존 스냅샷을 덮어쓴다.
두 번째 이동의 스냅샷은 첫 이동의 낙관적 상태이므로 실패 시 중간 상태로 돌아간다."*

리듀서를 `snapshot: existing?.snapshot ?? current`로 바꿔 해결하고, 테스트로 고정했다:
```
서류검토 → (빠르게) 면접 → 처우협의 → 첫 요청 실패
결과: 서류검토   ← '면접'(중간 상태)이 아니다
```

**3) 테스트 4번은 "고쳐서 통과시킨" 것이 아니다 — 시나리오가 도달 불가능해졌다**

이게 이 커밋에서 가장 조심한 부분이다.

- 원래 4번은 **"같은 카드에 두 요청이 동시에 떠 있는 상태"**를 전제로,
  늦게 온 응답이 최신 상태를 덮어쓰지 않는지 봤다.
- 직렬화가 들어간 뒤로는 그 전제를 **공개 API로 만들 수 없다.** 같은 카드의 두 번째
  요청은 첫 번째가 끝난 뒤에야 나간다. `moveCalls`는 항상 길이 1이 된다.
- 여기서 "expect를 느슨하게 고쳐 통과시키기"는 실격이다. 그래서 같은 불변식을 둘로 나눴다:
  - **(a) 도달 가능한 수준** — 통합 테스트에서 "세 번 눌러도 서버 요청은 하나",
    "대기 중 의도가 있으면 먼저 온 응답으로 확정하지 않는다"를 검증.
  - **(b) 큐 단위 테스트** — `moveQueue.test.ts`에서 응답 도착을 손으로 조작해
    순번 가드 자체를 검증. React 없이 돌아가므로 순서를 자유롭게 뒤집을 수 있다.
- 테스트 파일에 이 판단을 주석으로 남겨, 나중에 보는 사람이 "왜 원래 시나리오가
  없어졌는지" 알 수 있게 했다.

**4) 재시도 정책: 하지 않는다 (정한 것)**

- 409는 "내가 모르는 변경이 먼저 반영됐다"는 뜻이다. 내 의도를 새 version으로
  자동 재적용하면, **다른 사람이 방금 내린 결정을 조용히 덮어쓰게 된다.**
  채용 단계 같은 도메인에서 그건 위험하다.
- 그래서 **재동기화 + 알림**만 하고 대기 중이던 의도까지 버린다.
  사용자가 서버 상태를 보고 다시 판단하면 된다.
- "무한 재시도 금지"를 만족하는 가장 단순한 방법이기도 하다 — 재시도 경로 자체가 없다.
- 단위 테스트로 고정: `409 → onConflict 1회, 추가 요청 0건`.

**5) 내 큐 단위 테스트가 틀렸다 — 구현이 아니라 테스트를 고쳤다**

- `409 이후의 새 이동은 서버가 알려준 version으로 나간다`를 단정했다가 실패했다.
  큐가 409에서 받은 version(9)을 들고 있다가 다음 요청에 쓸 것이라 가정한 것이다.
- 실제로는 큐가 처리 후 비워지고 **스토어에서 version을 다시 읽는다.** 그게 맞는 설계다(1번).
  내 테스트 harness가 `onConflict`에서 스토어 갱신을 흉내내지 않아 1이 나왔다.
- **구현을 고치지 않고 harness를 실제 Provider와 같게 맞췄다**
  (`onConflict` → 스토어 version 갱신 = `MOVE_RESYNC`에 해당).
  검증 대상은 "재동기화 → 스토어 갱신 → 다음 요청 반영"의 연결이다.

**6) pending 잠금을 풀었다 — 잠가둘 이유가 사라졌으므로**

커밋 5~10에서는 이동 중 메뉴를 `disabled`로 잠갔다. 그건 경쟁 상태의 **완화책**이었고
커밋 5 기록에 "진짜 해결이 아니다"라고 적어뒀다. 큐가 들어와 연속 이동이 안전해졌으니
잠금을 제거했다. 진행 중임은 버튼 라벨로만 알린다.
(완화책을 남겨두면 "왜 잠겨 있는지" 아무도 모르는 코드가 된다)

**7) 큐 단위 테스트가 검증하는 것 (14개)**

| 항목 | 확인 |
| --- | --- |
| 직렬화 | 같은 카드 3연속 → 요청 1건 |
| 병렬 유지 | 다른 카드 2건 → 요청 2건, 각자의 기준 version |
| 병합 | 대기 중 의도는 마지막만, 건너뛴 단계를 `onCoalesced`로 보고 |
| version 전파 | 다음 요청이 직전 응답의 version을 실어 보냄 |
| 낡은 응답 차단 | 대기 의도가 있으면 도착 응답을 확정하지 않음 (`confirmed` 0건) |
| 네트워크 실패 | 남은 의도를 버리고 `onFailed` 1회, 추가 요청 없음 |
| 409 | `onFailed` 0 / `onConflict` 1 / 자동 재시도 0 |
| 재동기화 후 | 새 이동이 재동기화된 version으로 나감 |
| 없는 카드 | 요청 0건 |
| 10연속 | 서버 왕복 2회, 최종 확정은 마지막 의도 |
| 정리 | 처리 후 큐가 비워짐 |

**8) 브라우저에서 실제로 연속 클릭해 확인**

메뉴를 반복해 누르는 방식으로 6회 이동시킨 뒤 모든 요청이 끝나길 기다렸다.
```
마지막 의도: offer(처우협의)
화면:        처우협의
서버(localStorage): stage=offer, version=7
미해결 pending: 0
이력: 지원→screening / screening→interview / interview→offer / offer→interview / …
```
**화면과 서버가 일치**하고 pending이 남지 않았다.

- 이 실행에서는 **병합이 거의 일어나지 않았다.** 200장을 DOM 조회하는 시간 때문에
  클릭 간격이 요청 지연(200~800ms)보다 길어져 매 요청이 각각 완료된 것이다.
  이력에 6단계가 다 남은 게 그 증거다.
- 즉 **병합 경로는 브라우저에서 검증되지 않았다.** 그 부분은 큐 단위 테스트가
  결정론적으로 덮는다(10연속 → 왕복 2회). 브라우저 실행이 확인해 준 것은
  "여러 번 눌러도 최종 상태가 어긋나지 않는다"까지다. 둘을 구분해서 적는다.

**9) `stageHistory` 트레이드오프 (프롬프트에서 판단을 요구한 부분)**

병합된 중간 단계는 서버에 가지 않으므로 **이력에 남지 않는다.**
채용 이력은 감사 목적이 있어 손실이 바람직하지 않지만, 반대로
"0.2초 안에 스쳐 간 오조작"까지 이력에 남기는 것도 노이즈다.
후자를 택했고, 대신 `onCoalesced`로 호출부에 알려 개발 로그에 남긴다
(사용자 토스트로 띄우면 소음이라 제외).

**10) React Compiler 린트가 또 막았다 (5번째)**

`createMoveQueue`를 `useMemo`로 만들었더니
`Passing a ref to a function may read its value during render`.
`readVersion`이 `stateRef`를 캡처하니 렌더 중 ref 접근으로 판정될 수 있다는 지적이다.
실제로는 요청 시점에만 읽지만, 규칙을 끄는 대신 **생성 자체를 effect로 옮겼다**
(`queueRef`). 렌더에서 ref를 만지지 않게 되어 지적이 사라졌다.

**현재 상태**: `77 passed (77)` — 커밋 7에서 빨간 상태로 남겼던 테스트가 모두 초록이 됐다.

---

## [undo] 되돌리기

### 프롬프트 1

```
직전 이동 되돌리기를 만든다.

- 이동 성공 토스트에 "되돌리기" 버튼. 그리고 Ctrl+Z.
- Undo도 진짜 API 호출이다. 로컬만 되돌리면 새로고침 시 되살아난다.
- Undo도 15% 확률로 실패한다. 실패를 사용자에게 알려야 한다.
- Undo 대상은 "마지막 성공한 이동" 하나만.
  여러 단계 되돌리기는 범위 밖 — 왜 그렇게 정했는지 알려줘라.
- 낙관적 이동이 아직 확정되지 않은 상태에서 Undo를 누르면?
  이 케이스를 어떻게 처리했는지 명시해라.
- Undo 자체도 커밋 11의 카드별 큐를 타야 한다.
```

### AI 출력 요지

상태에 `lastMove`(id/name/from/to) 추가 — `MOVE_CONFIRMED` 시 `pendingMoves`의
**스냅샷**에서 `from`을 가져온다. `PendingMove.isUndo`로 되돌리기 이동을 표시해
확정 시 `lastMove`를 비운다(핑퐁 방지). 토스트에 액션 버튼 지원 추가,
`UndoHotkey`(Ctrl/Cmd+Z, 입력 중에는 무시), 되돌리기 테스트 12개.

### 리뷰 / 검증

**1) `from`을 어디서 가져오느냐가 핵심이었다**

- 순진한 구현은 이동 직전의 `applicant.stage`를 액션 생성부에서 기억하는 것이다.
  그런데 커밋 11에서 **연속 이동이 병합**되므로, 그렇게 하면 되돌리기가
  "중간 단계"로 돌아간다.
- `lastMove.from`을 `pendingMoves[id].snapshot.stage`에서 가져오게 했다.
  스냅샷은 **큐가 시작된 시점**이므로, 서류검토 → (병합) → 처우협의로 갔다면
  되돌리기는 서류검토로 간다.
- 테스트로 고정: `연속 이동이 병합됐어도 "처음 시작한 단계"로 되돌아간다`.
  이건 커밋 11의 스냅샷 설계가 있어야 성립하는 동작이다.

**2) "로컬만 되돌리기" 함정 — 브라우저에서 새로고침까지 확인**

```
강수희 서류검토 → (이동) 불합격 → (되돌리기) 서류검토
localStorage: stage=screening, version=3
이력 끝 2건: screening→rejected / rejected→screening
새로고침 후: 화면 서류검토, 서버 screening   ← 되살아나지 않는다
```
되돌리기가 로컬 상태만 뒤집는 구현이었다면 새로고침에서 `rejected`가 되살아났을 것이다.
version이 3까지 올라간 것도 되돌리기가 **진짜 서버 요청**이었다는 증거다.

**3) 되돌리기 실패 시 어디로 가는가 — 정하고 테스트했다**

- 되돌리기도 큐를 타므로 실패하면 `MOVE_ROLLBACK`이 스냅샷으로 되돌린다.
  이때 스냅샷은 "되돌리기를 시작한 시점" = **이동이 성공한 뒤 상태**다.
  즉 실패하면 되돌리기 이전으로 돌아간다. 이게 자연스럽다.
- 그리고 **`lastMove`를 비우지 않는다** — 확정될 때만 비우므로, 실패 후
  다시 되돌리기를 시도할 수 있다. 테스트 2개로 고정했다.

**4) 낙관적 이동이 진행 중일 때의 Undo — 거절한다 (프롬프트가 요구한 판단)**

- 진행 중인 이동을 되돌리기가 가로채면, 사용자가 **방금 누른 이동이 조용히 사라진다.**
  큐가 병합해 버리기 때문이다.
- 그래서 해당 카드에 `pendingMoves` 항목이 있으면 되돌리기를 받지 않고
  "이동이 진행 중입니다"로 알린다. 조용히 무시하지 않는 게 중요하다 —
  Ctrl+Z를 눌렀는데 아무 일도 안 일어나면 버그로 보인다.

**5) 되돌리기의 되돌리기(핑퐁) 차단**

되돌리기도 이동이므로 그대로 두면 `lastMove`가 다시 채워져 무한 왕복이 된다.
`PendingMove.isUndo`를 두고 확정 시 `lastMove = null`로 만들었다.
성공 토스트도 붙이지 않는다. 테스트 2개로 고정.

**6) Ctrl+Z를 입력창에서 가로채지 않는다**

검색창에서 Ctrl+Z는 "방금 지운 글자 되살리기"여야 한다. 그걸 뺏으면
사용자가 원래 기대하는 동작을 잃는다. `INPUT`/`TEXTAREA`/`contentEditable`에
포커스가 있으면 핸들러가 빠진다. 테스트로 고정
(`검색 입력창에 포커스가 있으면 가로채지 않는다`).
`Ctrl+Shift+Z`(redo)도 명시적으로 제외했다 — redo는 범위 밖이므로
그 조합을 되돌리기로 처리하면 오히려 혼란스럽다.

**7) 스택이 아니라 한 건만 두는 이유 (프롬프트가 요구한 설명)**

여러 단계 되돌리기는 "되돌릴 수 있는 시점"이 서버 상태와 어긋나기 쉽다.
그 사이 다른 사람이 같은 카드를 옮겼다면 스택의 중간 항목은 이미 무효인데,
그걸 사용자에게 정확히 설명할 방법이 마땅치 않다.
한 건이면 "직전 것만 되돌린다"는 규칙이 단순하고, 충돌 시 409로 걸러진다.
`LOAD_SUCCESS`에서 `lastMove`를 비우는 것도 같은 이유다 —
목록을 새로 받았으면 이전 되돌리기 대상을 더 이상 신뢰할 수 없다.

**8) 또 조용한 치환 실패 (커밋 10에 이어 두 번째)**

`onConfirmed`를 수정하는 문자열 치환이 **아무 경고 없이 넘어갔다.**
커밋 11에서 큐를 effect로 옮기면서 prettier가 들여쓰기를 8칸 → 6칸으로 바꿔
치환 대상이 안 맞은 것이다. 테스트가 "토스트가 없다"로 잡아줬다.
- 교훈이 반복됐다: **자동 치환은 성공했다고 가정하면 안 된다.**
  이후로는 치환 뒤 해당 부분을 직접 열어 확인했다.
- 이번엔 테스트가 있어서 30초 만에 잡혔다. 커밋 7~10에서 테스트를 쌓아둔 값이다.

**9) 테스트 12개가 검증하는 것**

되돌리기 동작 / 진짜 API 호출 / 실패 시 이동 후 상태 복귀 / 실패 후 재시도 가능 /
핑퐁 차단 / 병합된 이동은 시작 단계로 / 진행 중 거절 + 안내 / 재적재 시 대상 소멸 /
토스트 버튼 동작 / 되돌리기에는 버튼 미부착 / Ctrl+Z 동작 / 입력창에서 미가로채기.

**현재 상태**: `89 passed (89)`.

**미검증**: 실제 Ctrl+Z 키 입력. (브라우저 자동화가 특수키를 삼키는 문제가 그대로다.
jsdom에서 `user-event`로 동일 경로를 검증했다)

### 후속 수정 — 린트 에러를 커밋에 실어 보낸 실수

되돌리기 커밋을 밀어넣고 나서 `npm run lint`가 에러 1건으로 실패하고 있는 걸 발견했다.
**커밋 전에 lint를 확인하지 않은 것이 원인이다.** (build와 test만 보고 넘어갔다)

내용: `undoRef`(그리고 이어서 `queueRef`)가
`react-hooks/immutability` — *"effect 의존성으로 쓰인 값을 effect 안에서 수정할 수 없다"* 에 걸렸다.
구조가 이렇게 얽혀 있었다:

```
큐 생성 effect  ──(의존성)──▶ undoLastMove ──(읽기)──▶ queueRef
       └──────────────(대입)────────────────────────────┘
```

성공 토스트의 "되돌리기" 버튼이 `undoLastMove`를 참조해야 해서, 큐 생성 effect가
`undoLastMove`에 의존하게 됐고, 그 `undoLastMove`가 다시 `queueRef`를 만지면서 순환이 생겼다.
처음엔 `undoRef`라는 우회 ref로 끊으려 했는데 같은 규칙에 다시 걸렸다 — 우회가 아니라
구조 문제였다.

**해결**: 큐에 닿는 지점을 `startMove` **하나로 모았다.**
`moveStage`와 `undoLastMove`가 모두 `startMove`를 부르고, `undoLastMove`는 더 이상
`queueRef`를 모른다. 순환이 사라지고 코드도 짧아졌다.

되돌리기 자체의 동작은 바뀌지 않았다(테스트 89개 그대로 통과).
이 커밋은 되돌리지 않고 히스토리에 남긴다 — 검증 절차를 빠뜨렸다가 다음 커밋에서
잡은 흔적 그대로가 사실이기 때문이다.
