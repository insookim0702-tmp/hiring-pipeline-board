# DECISIONS.md — 설계 결정 기록

무엇을 골랐는지보다 **무엇을 버렸고 왜 버렸는지**를 적는다.
각 결정이 실제로 어떤 문제를 만들었고 어떻게 확인했는지는 [PROMPTS.md](./PROMPTS.md)에 있다.

---

## 1. 상태 관리 라이브러리를 쓰지 않았다

**결정**: TanStack Query를 검토했지만 채택하지 않고, `useReducer` + Context로 직접 구현했다.

**왜**

TanStack Query의 `onMutate` / `onError` 롤백은 **정확히 이 과제가 평가하려는 그 로직**이다.
라이브러리에 맡기면 "낙관적 업데이트를 구현할 줄 아는가"에 대한 답이
"설정할 줄 안다"로 바뀐다. 과제 명세에도 *"핵심 상태 로직을 통째로 대신해 주는
라이브러리에 의존한 경우 그 선택 이유를 문서에 밝힐 것"* 이라는 조항이 있다.

직접 구현해서 실제로 드러난 것들:

- 롤백 스냅샷의 **캡처 시점**이 복사 깊이보다 중요하다는 것
- 연속 이동 시 스냅샷을 덮어쓰면 **중간 상태로 롤백**된다는 것
- 409는 롤백이 아니라 **재동기화**여야 한다는 것 (내 스냅샷도 이미 낡았으므로)

라이브러리를 썼다면 세 번째는 기본 동작에 묻혀 인지하지 못했을 가능성이 높다.

**대가**: 캐시 무효화, 재시도 백오프, 포커스 시 재적재 같은 것을 얻지 못한다.
이 과제 범위에서는 필요 없었지만, 실제 제품이라면 그것들 때문에 라이브러리를 택할 만하다.

---

## 2. 낙관적 업데이트: 롤백과 재동기화를 코드에서 분리했다

**결정**: 이동 액션을 의미별로 넷으로 쪼갰다.

| 액션 | 언제 | 무엇을 |
| --- | --- | --- |
| `MOVE_OPTIMISTIC` | 사용자가 이동을 지시했을 때 | UI를 먼저 바꾸고 **반영 전 상태를 스냅샷으로 저장** |
| `MOVE_CONFIRMED` | 서버가 확정했을 때 | 서버 객체 전체로 교체 (version·이력 포함) |
| `MOVE_ROLLBACK` | 요청이 서버에 닿지 못했을 때 | 스냅샷으로 되돌림 |
| `MOVE_RESYNC` | 409 — 다른 변경이 먼저 반영됐을 때 | **스냅샷이 아니라 서버가 준 상태로** 맞춤 |

**왜 스냅샷을 리듀서 안에서 캡처하는가**

액션 생성부(Provider)에서 캡처하면 "낙관적 반영 전에 캡처한다"는 순서를 사람이 지켜야 한다.
리듀서 안에서는 인자로 받은 `state`가 곧 반영 전 상태이므로 **순서가 구조적으로 보장된다.**

**왜 진행 중이면 스냅샷을 유지하는가**

```ts
snapshot: existing?.snapshot ?? current
```

서류검토 → 면접 → 처우협의로 빠르게 옮긴 뒤 실패하면, 덮어쓰는 구현은
"면접"(중간 낙관적 상태)으로 되돌아간다. 롤백 기준은 **이 카드의 이동이 시작된 시점**이어야 한다.
되돌리기(Undo)가 "처음 단계"로 돌아가는 것도 이 스냅샷 덕분이다.

**왜 409에 스냅샷을 쓰면 안 되는가**

409는 "내가 알던 서버 상태가 이미 틀렸다"는 뜻이다. 그 시점의 스냅샷도 낡았으므로
되돌리면 오히려 남의 변경을 지운다. 그래서 서버가 응답에 실어 보낸 현재 상태로 맞춘다.

---

## 3. 드래그앤드롭 대신 명시적 이동 메뉴

**결정**: 과제가 허용한 택1에서 액션 버튼/메뉴를 골랐다.

**왜**

- DnD를 택하면 명세상 **키보드 대체 경로를 따로 구현해야 한다.** 버튼 방식은 그게 기본으로 따라온다.
- 접근성은 "대체 경로"보다 **하나의 경로가 모두에게 동작하는 것**이 낫다.
  마우스 사용자와 키보드 사용자가 같은 UI, 같은 상태 전이를 쓴다.
- 남는 시간을 경쟁 상태·롤백·테스트에 썼다. 이쪽이 과제가 실제로 보려는 지점이다.

**대가**: 칸반 보드다운 조작감이 약하다. 카드를 여러 장 한 번에 옮기는 흐름도 없다.

**시간이 더 있었다면**: 버튼 경로를 그대로 두고 그 위에 DnD를 얹었을 것이다.
버튼 경로가 이미 있으므로 "키보드 대체 경로"가 자동으로 확보된다.

---

## 4. mock 서버를 localStorage 뒤에 두고, 실패는 "쓰기 전"에만 일으킨다

**결정**: 모듈 메모리를 1차 저장소로 두고 쓰기가 일어날 때만 `localStorage`에 직렬화한다.
실패 판정은 저장소를 건드리기 **전에** 한다.

**왜 localStorage인가**

과제 요구가 "이동이 mock API에 저장되어 새로고침 후에도 유지"다. 메모리만 쓰면
새로고침에서 되살아나고, 그건 mock API 구현에서 가장 흔한 누락이다.
그래서 **UI 경로와 단위 테스트 양쪽에서** 새로고침 유지를 확인했다.

**왜 요청마다 JSON을 파싱하지 않는가**

1,000건을 매 요청 파싱하는 건 "서버"가 할 일이 아니다. 메모리에 한 번 올려두고
쓰기 시에만 저장한다. 밖으로 나가는 객체는 복사해서 넘겨 저장소가 오염되지 않게 했다.

**왜 실패를 쓰기 전에만 일으키는가**

실패에는 두 가지가 있다 — (a) 요청이 서버에 닿지 못함 (b) 서버는 처리했는데 응답이 유실됨.
(b)까지 흉내내면 클라이언트가 반영 여부를 알 방법이 없어 **낙관적 롤백이 정답 없는 문제**가 된다.
과제 요구는 "실패 시 원상 복구"이므로 (a)로 한정했다.
`실패한 요청은 서버 상태를 바꾸지 않는다`를 테스트로 못 박았다.

---

## 5. 가상 스크롤만 라이브러리를 썼다

**결정**: `@tanstack/react-virtual`. 그 외 런타임 의존성은 `react` / `react-dom` 뿐이다.

**왜 허용 범위라고 봤는가**

이 라이브러리는 **무엇을 그릴지**만 정한다. 지원자 상태, 낙관적 반영, 롤백, 요청 순서는
전혀 건드리지 않는다. 과제가 경계한 "핵심 상태 로직을 통째로 대신해 주는 라이브러리"에
해당하지 않는다.

**직접 구현하지 않은 이유**

동적 높이 측정 + 스크롤 앵커링을 직접 짜면 그 자체로 하루가 든다.
그 시간에 얻는 건 이 과제가 평가하지 않는 영역이다.

**대신 치른 비용**: 가상화가 커밋 10의 키보드 이동을 깨뜨렸고, jsdom에서 테스트가
전부 무력화됐다. 두 문제 모두 해결했다(→ PROMPTS.md `[virtualization]`).
라이브러리를 쓰면 공짜인 줄 알았던 부분이 실제로는 공짜가 아니었다.

`react-hooks/incompatible-library` **경고** 하나가 남아 있다. React Compiler가 이
라이브러리의 구독 방식을 최적화 대상에서 제외한다는 안내이고, 동작에는 영향이 없다.

---

## 6. 경쟁 상태: 카드별 큐 + 자동 재시도 없음

**결정**: 요청 큐를 리듀서 밖 순수 모듈(`moveQueue.ts`)로 뺐다.
같은 카드는 직렬, 다른 카드는 병렬. 409에 **자동 재시도하지 않는다.**

**왜 리듀서 밖인가**

응답 도착 순서를 뒤집어 가며 검증해야 하는데, 그건 컴포넌트 트리를 세우지 않는 쪽이 훨씬 명확하다.
실제로 큐 단위 테스트 14개가 React 없이 돌아간다.

**왜 큐가 version을 직접 들고 가는가**

큐는 응답 직후 곧바로 다음 요청을 보낸다. 그 시점에는 React가 아직 커밋하지 않아
스토어를 읽으면 낡은 version이 나오고, 두 번째 요청이 무조건 409가 된다.
큐가 응답에서 받은 version을 이어 쓰면 이 타이밍 의존이 사라진다.
다만 큐는 다 처리하면 **스스로 비운다** — 무기한 들고 있으면 다른 경로로 바뀐
서버 상태를 영영 모르게 된다.

**왜 자동 재시도를 하지 않는가**

409는 "내가 모르는 변경이 먼저 반영됐다"는 뜻이다. 내 의도를 새 version으로 자동 재적용하면
**다른 사람이 방금 내린 결정을 조용히 덮어쓴다.** 채용 단계 같은 도메인에서는 위험하다.
재동기화하고 알린 뒤, 사용자가 서버 상태를 보고 다시 판단하게 한다.
"무한 재시도 금지"도 자연히 만족한다 — 재시도 경로 자체가 없다.

**대가 — 이력 손실**: 대기 중 의도를 병합하므로 중간 단계가 `stageHistory`에 남지 않는다.
채용 이력은 감사 성격이 있어 손실이 바람직하지 않지만, 0.2초 안에 스쳐 간 오조작까지
남기는 것도 노이즈다. 후자를 택했고 `onCoalesced`로 개발 로그에 남긴다.

---

## 7. 스크린리더 안내를 토스트에서 분리했다

**결정**: `aria-live` 영역은 `AnnouncerProvider` 하나뿐이다. 토스트는 시각 표현만 담당한다.

**왜**

처음에는 토스트에 `role="alert"`을 달아 안내까지 겸하게 했다. 두 가지가 걸렸다.

- **성공한 이동은 토스트를 띄우지 않던 시점에도 안내는 해야 한다.**
- 되돌리기 토스트가 생기면 같은 사건이 토스트와 live region에서 **두 번 낭독**된다.

채널을 하나로 모으면서 커밋 7의 롤백 테스트가 깨졌고, 검증 대상도 새 채널로 옮겼다.
채널을 바꿨으면 테스트도 따라가야 한다.

live region 컨테이너는 **항상 DOM에 둔다.** 메시지와 함께 마운트하면 스크린리더가
변경을 감지하지 못해 낭독하지 않는 경우가 많다. 테스트로 고정해 뒀다.

---

## 스스로 세운 가정 (요구사항이 모호했던 부분)

| 가정 | 근거 |
| --- | --- |
| 단계 순서는 서류검토 → 면접 → 처우협의 → 최종합격 / 불합격 **일방향이 아니다** | 명세에 역방향 금지가 없고, 실무에서 되돌리는 일이 잦다. 어느 단계로든 이동 가능하게 했다 |
| 지원자 생성·삭제는 범위 밖 | 명세의 요구는 "단계별로 관리"와 "단계 간 이동"뿐이다 |
| 상세 정보는 읽기 전용 | 메모 편집은 요구에 없다. 넣으면 낙관적 업데이트 대상이 하나 더 늘어 초점이 흐려진다 |
| 필터를 URL에 반영하지 않는다 | 새로고침이 곧 mock 서버 재적재라 공유 실익이 적고, 검증용 파라미터(`?count`·`?fail`)와 섞이면 혼란스럽다. 실제 서비스라면 반영하는 게 맞다 |
| 되돌리기는 1단계만 | 아래 참조 |
| 다중 사용자 동시 편집은 없음 | mock 서버가 단일 브라우저 localStorage다. 다만 409 경로는 실제로 구현·검증했다 |

---

## 못 끝낸 것과 범위 밖으로 둔 것

### 의도적으로 하지 않은 것

- **드래그앤드롭** — 위 3번. 택1이 허용되어 버튼 방식을 골랐다.
- **되돌리기 스택(여러 단계)** — 그 사이 다른 변경이 있으면 스택 중간 항목이 무효인데,
  그걸 사용자에게 정확히 설명할 방법이 마땅치 않다. 1단계면 규칙이 단순하고
  충돌은 409가 걸러낸다. 같은 이유로 목록을 새로 받으면 되돌리기 대상을 비운다.
- **다시 실행(Redo)** — 되돌리기의 되돌리기는 핑퐁이 된다. `Ctrl+Shift+Z`를 명시적으로 제외했다.
- **필터 URL 동기화** — 위 가정 참조.

### 확인하지 못한 것 (추측으로 채우지 않았다)

- **실제 스크린리더 낭독** — 접근성 트리의 이름·역할과 live region 존재, 낭독 문구가
  들어가는 것까지는 확인했다. NVDA/VoiceOver로 실제 낭독 순서와 억양은 듣지 못했다.
- **실제 방향키 / `Escape` / `Ctrl+Z` 키 입력** — 사용한 브라우저 자동화 도구가 이 키들을
  자체적으로 삼켜서(`document`에 keydown 프로브를 달아 **도달 0건**을 확인했다)
  브라우저에서 검증할 수 없었다. jsdom + `user-event`로 같은 코드 경로를 검증했고,
  Tab·클릭·타이핑은 실제 브라우저에서 확인했다.
- **연속 클릭 병합의 브라우저 재현** — 실제 클릭 간격이 요청 지연(200~800ms)보다 길어져
  병합이 발동하지 않았다. 큐 단위 테스트가 결정론적으로 덮는다(10연속 → 왕복 2회).

### 시간이 더 있었다면 다음에 했을 것

1. 버튼 경로 위에 드래그앤드롭 얹기 (키보드 경로는 이미 있다)
2. 실기기 스크린리더 검증
3. 여러 카드 일괄 이동 (큐가 이미 카드 단위 병렬이라 확장 지점이 있다)
4. `stageHistory` 손실 없는 병합 — 병합된 중간 단계를 클라이언트에서 이력에 합성
